# 更新日志

本项目遵循语义化版本（SemVer）。

## [未发布]

- 顶部版本入口新增版本与更新窗口，按需查询正式Release，发布说明以安全纯文本展示；服务端合并并发请求、缓存6小时、限制手动刷新频率并保留失败前的成功结果。dev和本地构建仅展示正式版参考，不判断dev镜像更新，不自动更新或重启。

- 主页调整为账号、任务中心、设置；设置使用六个原生折叠分组并保留统一保存，校验失败展开相应设置。看板长报错可展开查看，保留当前阶段与恢复按钮；手机规格选择将参数、大小来源和字节数分层，统一次要按钮与工作区关闭控件。

- 待处理详情隔离主页网格样式，收拢重复信息并前置文件保护说明；媒体探测按钮统一玻璃样式，隐藏旧编码编辑器及空提示条，精简手机规格说明。源站长原因保留在详情，状态标签使用短文案；业务流程和主页布局不变。

- 源站诊断保留审核中（62004）与仅UP主可见（62012）的明确原因，仍沿用未知状态的复核节奏；稿件不可见（62002）和未找到视频（-404）不再被笼统解释为作者删除。归档库和收藏详情补充准确文案，不增加请求、不重置已有任务或影响旧归档。

### 修复

- 互动视频下载使用BBDown完整可达剧情片段清单，不再仅把详情接口的根片段作为全部分P；清单与实际下载通过CID顺序摘要绑定，变化时停止下载。已完成片段继续保留，重试只补缺失片段。
- 互动剧情解析失败独立于“B站源不可用”；结构化媒体探测缺少片段时拒绝返回不完整大小，避免错误显示完整归档或低估大小。
- 开发分支固定BBDown为已发布的`bfb-2.0.5`（`fa7209d`），校验Linux x64发布包SHA256。`2.0.4`及`2.0.3`在账号、接口与运行配置不变时保留续传轨道；更早APP轨道仍沿用隔离规则。不支持互动清单协议时安全失败，不覆盖已有清单。BFB版本及所有schema不变。

## [2.5.4] - 2026-09-06

### 改进

- 视频不可用改为可自动复活的软终态：收藏夹首次报告失效时先暂停下载并错峰确认，只有两个详情接口都明确返回不存在或不可见才判定失效；随后按1天、7天、30天低频复核并进入休眠，重新可见后自动恢复原有幂等归档流程。已有归档、封面和历史关系始终保留。
- 充电权限复核与视频可用性复核继续共用唯一的 `access_probe:<BVID>` 持久任务，并分别统计任务意图。普通可用性探测仅使用UP主账号和活动收藏关联账号，按UID去重；充电探测保留其他启用账号回退。用户立即复核、账号重新登录、数据库重载和周期恢复都不会生成重复探测。
- 重命名检查改为服务端短期预览会话：前端只提交不可猜测的 `previewId` 和 `candidateId`，源路径、访问别名、文件大小及当前存储身份留在服务端；旧式直接提交 `oldPath/newPath` 的请求不再进入远端操作。
- 重命名预览轮询改为按 `previewId + revision` 增量读取；远端深扫增加条目数、目录数和重复目录边界。完整扫描结果会权威替换本地候选，未完成扫描只保留本地已验证证明，避免把部分扫描结果当成全量事实。
- 在线内容页面缓存只缓存 B 站内容本身，命中缓存时重新从 SQLite 批量附加归档状态；手动归档、队列变化或状态恢复后无需重新请求 B 站即可反映最新状态。

### 修复

- 修复无关账号登录失败影响普通视频可用性判断的问题；手动复核没有相关可用账号时返回明确提示。
- 已归档的收藏失效文案与归档结果分开表达，不再宣称没有安排的后台复核；修复详情内复核按钮的Enter/空格误触外层播放。
- 路径迁移弹窗关闭会取消读取并拒绝迟到响应，轮询串行执行；新增按需分页的冲突与失败详情、重试和处理建议，不额外探测目标文件大小。

- 修复已停止上传仍残留人工处理标记而重新出现在待处理和看板的问题。启动、数据库重载和周期恢复分批修正矛盾字段；停止尝试同时清除重试计划，旧回调不能重新唤醒。停止按会话代次保留，不阻止同视频的新尝试，也不删除本地文件或归档证明。
- 补齐编码替换父任务的迟到回调保护：即使历史记录残留“替换运行中”，停止后的完成、进度和重新开始入口也不能重新打开人工处理或创建子任务。

- 传输恢复收口：创建上传代次前先验证本地文件，代次、分P清单与任务绑定在同一事务建立；远端只读确认不再依赖本地文件存在。普通上传、编码替换和画质重调的关键阶段使用同一SQLite事务提交下游任务与当前任务状态，空代次恢复只在旧代证明重新核验一致时结束，不把旧代成功当成新代成功。
- 本地文件保护：上传函数不再直接删除整个下载目录，清理必须显式指定清单路径；删除失败或文件大小变化时保留清单记录。严格重试失败与源不可用不再触发候选自动清理，已停止任务不再被旧恢复归一化重新打开。
- 本地清理授权保存在现有任务JSON中，绑定清单、传输代次和逐文件身份，重启后可继续收尾；同大小替换、新任务引用或代次变化时停止清理。待清理记录不占用上传队列和原业务去重键。全部分P已确认但尚未收尾的会话改为只读确认，不再误显示人工失败。
- 新增归档详情“释放本地空间”：已验证文件沿用成功清理授权；失败或停止后保留的文件可独立预览并输入 `DELETE LOCAL` 明确删除，展示有无归档及唯一副本风险。运行任务须先停止，旧页面、目录复用或同大小替换会拒绝删除。
- 修复共享编码重试的首个子任务成功后提前结束父任务的问题，同代其余子任务完成前保留父任务；修复多文件清理发现清单变化后仍可能用旧快照覆盖清单的问题。
- 修复明确不可用的下载仍可能被普通重试或恢复投影重新创建的问题；当前下载立即结束且不创建上传任务，严格编码/画质候选也不会替换旧归档。BBDown 的 `Arg_KeyNotFound`、EP/SS参数问题和普通工具故障不再被误判为视频永久不可用。
- 修复旧版不可用分类恢复时可能清空同一BV的WebDAV、权限或本地文件错误；现在只恢复带明确视频源不可用证据的收藏关系和永久失败记录，手动归档来源不受自动可用性状态影响。
- 修复重命名预览在深扫产生新结果时重建完整列表、重置用户勾选的问题；候选跨 revision 保留选择状态，消失候选会被移除，迟到请求不会覆盖当前预览。
- 修复 OpenList 特殊文件名的服务端访问路径在安全重命名流程中丢失的问题；执行阶段继续重新核验路径、类型和大小，仍不会把远端路径交给前端控制。

## [2.5.3] - 2026-08-28

### 改进

- 优化“检查旧命名文件”和“检查可升级画质”的预览路径：重命名检查先使用SQLite中已有的已验证远端证明即时生成候选，远端目录深扫改为后台有界并发（最多2个目录请求）并通过短时缓存补充遗漏；执行重命名时仍会逐项重新核验远端，不会把本地索引直接当成远端事实。
- 预览跳过项改为保留完整总数和按原因统计，只返回少量示例供页面展示；画质重调队列目标改为一次批量读取，避免对每个收藏关系重复查询SQLite。

### 修复

- 修复“检查可升级画质”被失效或尚无远端文件证明的收藏关系整体中断的问题。预览现在先判断关系是否具备最终确认的归档，再校验全部文件路径；空证明、越界路径、多目录证明或旧目录不一致只跳过对应视频并给出原因，不再对空路径执行目录解析，也不会伪造可替换目标。
- 修复OpenList目录清单用反斜杠转义撇号或百分号时，“检查旧命名文件”报“远端路径包含非法字符”的问题。BFB继续只持久化严格逻辑路径，扫描和执行阶段把OpenList实际访问写法保留为服务端临时值；特殊名称必须通过目录、文件名和大小唯一匹配本地证明，执行前再次解析，歧义或证明冲突时安全跳过。普通严格路径保持原有检查行为，全局路径校验没有放宽。

## [2.5.2] - 2026-08-26

### 改进

- 前端视觉收敛为统一的运维工作区：登录页、首页分区、普通弹窗、任务看板、归档库、在线内容和待处理中心共用浅青绿色玻璃材质、柔和背景光晕、`6/8/10px`控件层级及一致的焦点样式。普通弹窗统一为四档尺寸、固定标题区和粘性操作区；危险操作保持独立层级，播放器继续保留深色沉浸界面。
- 主页外层分区与主页操作按钮的圆角略微加大，设置区的空状态提示行改为仅在有内容时占位，减少操作按钮下方的无意义留白；弹窗、播放器和归档库内部控件保持原有层级。
- 待处理中的媒体替换改为统一的“重新选择画质与编码”：先按真实账号探测当前全部分P共同可用的B站画质×编码组合，显示分辨率、帧率、预计成品大小、本地峰值、缓存余量和大小来源，再对用户选中的组合执行一次精细探测。画质或编码可以单独设为严格项，未指定维度沿用任务设置或偏好；探测失败时不预选任何规格，必须由用户明确选择后才会严格尝试。
- 队列看板对严格媒体替换失败使用与待处理中心一致的安全摘要，入口简化为“重新确认 / 换规格”，不再展示底层英文上传错误，也不提供不适用的“继续上传”捷径。

### 修复

- 收口普通弹窗的玻璃表面：标题区和底部操作区不再单独叠加半透明白色与模糊层，避免登录、收藏夹等弹窗上下出现明显白带；在线内容桌面端的刷新与关闭按钮也固定归入右侧操作区，并移除重复的读取说明文字。
- 修复“选择同步收藏夹”列表缺少封面的问题：收藏夹详情封面现在按可见项懒加载，并复用有界的`data/online-covers`缓存；缓存命中时不会重复请求B站或重新转码，封面失效时仍回退为占位图。
- 修复待处理中心空状态的展示边界：没有需要管理员决定的事项时不再显示无意义的“自动复核中”徽标，空状态内容保持在同一工作区背景内；桌面弹窗保留安全边距，手机端仍保持全屏布局。
- 修复在线内容工作区的关闭控件和收藏夹数量展示：桌面端只显示内容区关闭按钮，手机目录页保留侧栏关闭按钮；收藏夹优先显示 B 站返回的真实视频数量，页面有总数时同步显示加载总量，不再误显示“打开时加载”。
- 修复前端长请求与弹窗生命周期错位的问题：收藏夹、在线内容、归档库、登录轮询、设置和账号列表都使用可取消请求与不可变上下文，迟到响应不能覆盖新账号、新搜索或新弹窗；切换归档目录期间旧卡片不可操作，失败后恢复最后一次已生效的目录和分页状态。
- 隐藏弹窗现在会从布局和键盘焦点树中移除；保存收藏夹、保存设置、账号启停/刷新和同步命令增加单操作互斥与失败恢复。账号启停改为提交明确的`enabled`值，后端继续兼容旧版`toggle`请求，快速双击不会反转两次。
- 修复在线内容、归档库、收藏夹详情和播放器搜索在单页返回重复条目时可能渲染重复卡片的问题；可复用的手动归档弹窗每次打开都会恢复可操作状态；初次设置或账号读取失败会显示可重试状态，不再留下空白界面或未处理Promise异常。
- 修复在线内容适配器把统一页大小直接传给B站不同接口，以及历史全量游标使用错误业务值的问题；接口现在按各自限制请求，在线缓存仍保持有界。
- 修复在线缩略图并发槽位交接和清理/提升互斥可能留下等待任务或形成死锁的问题。
- 严格媒体规格重试现在同时校验B站画质档位与ffprobe实际编码；任一分P不匹配、缺失证明或旧任务在PUT前复核失败时都不会上传候选或改变原归档。
- 收口候选级恢复状态：启动、数据库重载、周期复核和打开待处理中心都会把没有对应`jobs`记录的活跃上传会话投影为唯一的`upload-session:<sessionId>:g<generation>`恢复任务；失败候选不会重复生成，终态任务在会话仍未完成时也能重新出现，迟到的旧代次不能覆盖当前候选。
- 远端异大小冲突、多分P混合状态和达到观察门槛的长时间不可见现在自动写入隔离候选；正式路径、旧归档和原有证明保持不变。候选本身失败后停止自动重试，回到待处理中等待用户处理，避免候选生成循环。
- WebDAV 405/写入异常增加脱敏证据：状态码、`Allow`、`Retry-After`、内容类型/长度、DAV及有限的`X-OpenList-Error-Code`等白名单字段会随恢复任务保存；不读取OpenList日志，不保存认证头、Cookie、签名地址或完整远端路径。明确的单文件限制错误码才会分类为大小限制，普通405仍保持未知写入拒绝。
- 待处理候选新增幂等的“放弃本次候选”操作：只结束当前候选并保留本地文件、隔离远端文件和旧归档证明，不执行远端删除；后续同步仍可创建新的候选代次，已放弃的旧候选不会在每轮同步中反复出现。
- 修复恢复投影只检查前1000个传输会话导致后续孤儿会话没有待处理入口的问题；现在按分页扫描并以`sessionId:generation`去重，部分分P明确显示`partial_upload`及已确认/总分P进度，投影阶段不再同步阻塞读取本地文件。
- 修复旧版下载失败记录与新恢复中心脱节的问题；仍有效且未明确放弃的失败会话会投影为唯一下载恢复项，用户放弃后保留审计记录但不再重复刷屏。
- 优化封面读取生命周期：在线内容的懒加载引用不再早于24小时浏览器缓存失效，收藏夹列表短时复用并合并并发请求，列表接口已返回的封面会直接预热在线缩略图缓存；归档封面只把可读非空文件视为有效，避免空文件阻塞回填。

## [2.5.1] - 2026-08-25

### 新增

- 新增“在线内容”工作区：按账号按需读取在线收藏夹、订阅合集、追番追剧、稍后再看和历史记录；已归档BVID直接复用本地归档库，不在普通同步周期增加B站请求。
- 新增手动归档来源，使用独立的`source_kind=manual`和`media_id=-1`，复用下载、上传、恢复、冲突候选、远端验证、账号移除和安全删除状态机；重复点击与重启保持幂等。
- 封面分为永久归档封面`data/covers`和有界在线缩略图`data/online-covers`。在线缩略图默认256MB、按LRU淘汰到90%水位；归档封面不因缓存压力或临时文件清理自动删除，视频失效后仍可显示。
- BBDown fork 更新至`bfb-2.0.4`，新增不含签名URL的`BFB_PROBE_JSON`媒体探测协议；BFB可展示分P组合、估算大小和可用编码，严格任务逐分P核对画质与编码，ffprobe仍是最终落盘依据。
- SQLite升级到`user_version 11`；JSON状态schema 13和迁移包schema 3保持不变。回滚前须恢复schema 10升级备份，旧镜像不能直接打开schema 11数据库。

### 依赖维护

- BBDown fork 更新至 `bfb-2.0.4`，固定源码提交 `0ea9463202e8a57e0d673f29166e54f4ed770255`；新增隐藏的`--bfb-probe-json`结构化媒体探测开关，并对严格目标组合使用 HEAD/Range 返回精确大小。`2.0.3`未完成WEB与APP轨道可继续续传，更早APP轨道仍按既有安全规则隔离。

### 修复

- 媒体探测协议收紧为整批校验：任一分P记录损坏、BVID不符或页码/CID重复都会让本次探测安全失败，不再用部分页面低估空间。BBDown协议v2同时报告实际选中视频和音频的大小来源，只对排序后首个严格目标并发细化大小；畸形`206`不会把单字节响应长度误当成完整文件，旧协议v1继续显示“仅视频流估算”。
- 手动精确归档的严格画质与编码目标会随持久上传任务、验证任务和人工恢复链路保存，并在首个WebDAV请求前重新读取下载清单核验；应用重启或旧任务恢复不能绕过严格目标。普通同步没有该目标，因此不增加校验或网络请求。
- 严格编码重试增加三道真实落盘保护：BBDown 明确选中错误编码时提前停止；下载清单在 `ffprobe` 确认所有分P编码匹配前不会进入 `complete`；调度器入队和 `UploadTask` 发起任何 WebDAV 请求前会再次核验。编码不匹配或无法确认时不创建上传候选、不修改旧归档证明，待处理会显示请求编码、实际编码和已验证分P数；普通下载与非严格编码优先级仍允许 BBDown 自动回退。
- 待处理恢复流程按证据分级：远端文件至少3次独立观察且持续30分钟不可见后才升级为人工项；本地整组文件完整、父目录可读且不是认证/网络故障时，可由管理员生成稳定的`_conflicts`隔离候选。多分P始终整组上传，正式旧路径不会覆盖、移动或删除；只有完整旧档与完整候选并存时才要求选择，旧档不完整时自动采用已验证候选，候选不完整时自动保留完整旧档。
- 下载重试耗尽后不再只留下失败关系：可恢复的任务持久进入“待处理”，支持原账号重试、先验证备用账号再重试或暂缓24小时；恢复只重置本任务的目标来源，不会降级同一BV的其他已验证归档，暂缓期间保留原失败状态和本地进度。明确下架/不可用仍按终态处理。画质重调只有在下载/暂存上传证据明确指向编码或单文件限制、且正式替换尚未开始时，才提供一次性“换编码重调画质”，目标画质和旧归档保持不变。
- 全局设置新增只读WebDAV连接检查，直接使用尚未保存的地址、账号、密码和归档路径，最多等待15秒；检查只执行`PROPFIND/stat`，不会创建、上传、移动或删除文件，也不会把“可读取”表述为“已验证可写”。
- 新增可持久化的 BBDown 编码偏好排序，默认顺序为 `HEVC → AVC → AV1`；设置只影响之后创建的下载任务，不改写已有成品或媒体元数据。远端单文件限制等编码相关下载不再伪装成成功：任务会继续保留在“待处理”，可单独选择一次性编码顺序（默认 `AV1` 优先并严格使用首项）在隔离目录重下，原文件直到新文件完成上传和远端确认后才会处理；过期的旧回调不会覆盖新状态。
- 修复普通上传达到重试上限后只停留在 SQLite `failed`、不进入“待处理”中心的问题；耗尽的 `upload/history_upload` 任务现在保留本地文件并持久化人工恢复标记，启动时也会补标记旧版本遗留的失败任务。识别到 OpenList / WebDAV 的 `SingleFileSizeOverLimit` 时立即暂停当前文件，显示“远端单文件超过存储限制”，不打开全局上传熔断、不重复尝试兼容文件名或后台重传，用户调整存储设置后可从待处理中继续。
- 收紧 AList / OpenList 远端错误分类：HTTP 状态优先于响应正文，500 中包含“not found”不再被误判为缺失；没有HTTP状态时仅把明确的 `ENOENT` / `ENOTDIR` 视为缺失，其余保持未知并进入安全恢复路径。
- 画质升级和远程重命名不再在启动或普通任务创建时写入远端探测文件；未知的 COPY / MOVE 能力改为在真实操作中按目标类型、大小和源文件消失状态懒验证，确认不支持后才允许 COPY+DELETE 降级。路径迁移的目录级 `DELETE` 行为保持不变。
- 账号远端清理新增持久 `config_removing` 阶段：配置文件写入与SQLite任务裁剪分离，配置已经移除后即使后续收尾失败也不会把账号凭据重新写回；启动和数据库重载可继续恢复该阶段。
- 上传结束后增加轻量本地文件复核，检查实际打开文件的设备/inode（平台可用时）、大小、修改时间和变更时间；不计算哈希，异常时保留本地文件并进入现有重试/人工恢复流程。
- 修复 OpenList 默认目录清单中合法文件名包含字面 `#` 或 `?` 时被误判为 URL 查询串/片段的问题。`filename`、`path`、`basename` 和 `name` 按结构化远端字段解析，只有 `href` 使用 URL 语义；混合目录中的无关坏条目会被逐条隔离，疑似目标的坏条目仍安全阻断。全量扫描遇到目录读取或条目解析异常会标记为不完整，不再生成可执行的完整扫描结果。
- 修复来源级归档删除在进入运行阶段后丢失账号、收藏夹和BVID上下文的问题。共享下载和共享画质任务现在只裁剪正在删除的来源，其他账号来源继续保留；没有剩余目标的任务不会再生成上传或验证工作。
- 修复来源删除期间已运行下载完成后仍可能把成品重新上传到待删除来源的问题；任务恢复、质量升级恢复、上传目标收集和新的上传入队都会重新检查来源删除状态。
- 修复多目标任务被错误放行的竞态：只要任务目标集合中包含正在删除的来源，相关下载、上传、验证和质量替换不会启动；其他来源不受账号级全局暂停影响。
- 修复来源删除准备阶段裁剪持久任务后，重启或任务租约恢复仍携带旧目标的问题；共享任务会同步保存裁剪后的目标集合，删除失败或重试仍保留剩余来源的可播放证明。
- 远端文件观察统一为“存在 / 缺失 / 未知”三态：只有明确的 `404`、`ENOENT` 或 `ENOTDIR`，以及成功目录列表确认无匹配时才判定缺失；`400`、`405`、权限、冲突、限流和网络异常不再触发重复上传或清除证明。
- 针对部分 OpenList / 天翼驱动把写入拒绝压缩成通用 `405` 的情况，上传后会记录“目标缺失但父目录可见”的有限证据；连续尝试仍无法确认时进入“远端写入结果未确认”，保留本地文件并在“待处理”提供一次隔离换编码重试。系统不会把它直接宣称为大小限制，也不会对权限、路径、网络或父目录不可见的错误开放该操作。
- 上传、复制、移动和删除后只失效受影响父目录的本地目录缓存；真实操作中学习同一 WebDAV 后端的 COPY/MOVE/扩展请求头能力，能力画像按地址、账号、密码和归档根隔离，重启后安全重新学习。
- 修复来源重新出现在收藏夹时仍可能被正在运行的删除任务继续处理的问题；删除 worker 在关键步骤重新检查 `superseded` 状态，已确认消失的单文件才收尾，未确认的远端证明继续保留。
- 修复账号远端清理事务回滚后残留内存上传任务的问题；任务计划先随SQLite事务提交，提交后才裁剪内存队列并唤醒调度器，账号删除等待范围也缩小到目标账号和相关共享任务。
- 修复路径迁移取消、预览和迁移包导入之间的换库竞态；导入前会等待预览和worker真正空闲并取得生命周期屏障，超时返回冲突，不替换仍被旧任务持有的SQLite连接。路径迁移既有目录级 `DELETE` 行为保持不变。
- 播放端遇到远端 `HEAD` 的 `405/501` 时仅回退一次 `GET Range: bytes=0-0` 读取响应头，不读取完整视频；上传扩展时间头被拒绝时仅在可靠确认目标未落盘后重试一次标准PUT。
- 归档删除改为分批读取远端路径，完成收尾使用预览时持久化的文件数和字节数，不再因证明已清理而把审计统计写成零；来源在实际 DELETE 期间重新加入收藏夹时只收尾已确认删除的文件，并保留活动关系等待补传，关系记录已消失时也能完成删除审计。
- 上传、替换和迁移共用远端目录准备与观察逻辑；保留常规 `exists()` 快速路径，stat-only 客户端仍会拒绝“文件冒充目录”，兼容名称覆盖 Unicode 规范化、保留字符和过长 UTF-8 名称，并在确认目标连续缺失后才允许扩展头回退。
- 播放代理对每个外部 HTTPS 跳转固定到已通过私网地址检查的 DNS 结果，避免校验与实际连接之间的解析漂移；重命名、删除、上传和诊断响应中的远端路径改为 basename 加短摘要，不返回完整路径或凭据。
- 统一弹窗的滚动锁、后台隔离、可访问名称和提示层级：普通弹窗打开后页面不再在背景滚动，错误提示会进入当前顶层弹窗，嵌套确认仅保留一个 `aria-modal` 所有者，关闭后恢复原滚动位置和触发焦点。
- 修复手机归档库不可见目录或内容面板仍可被键盘聚焦的问题；两级页面、详情面板和视口切换会同步更新 `inert` / `aria-hidden`。设置字段补齐显式标签，归档元信息提高对比度，粗指针控件扩大触控目标，并完整尊重减少动态效果偏好。
- 修复旧版 BBDown 遗留的永久下载失败记录无法收敛的问题：活动收藏关系现在会在“待处理”中心显示脱敏的人工恢复项，只有用户选择“重新下载”或“换账号下载”后才建立新的持久化任务，不会按同步周期自动重复尝试；自有稿件恢复详情日志也只在状态真正变化时记录一次。

## [2.5.0] - 2026-08-19

### 新增

- 任务日志旁新增“待处理”问题中心，按“发生了什么、系统保护了什么、推荐处理、技术详情”展示上传恢复、画质重调和存储配置问题；提供脱敏诊断摘要、键盘焦点管理及桌面/手机两级布局。
- 上传人工等待增加低频自动自愈：远端文件已按期出现时自动确认；远端确实缺失且本地补传文件失效时最多自动重新下载3次。异大小、混合分P状态、认证失败或无法安全重建时保持暂停并交给问题中心，不自动覆盖或删除远端文件。
- 远端存储配置和文档明确支持 AList 与 OpenList WebDAV；保留 `alistUrl`、`alistBrowserUrl` 等旧字段名，已有配置无需迁移。设置页、播放器入口、状态对账、路径迁移和排障说明统一使用“远端存储”或“AList / OpenList”。
- 归档路径迁移开始前增加隔离临时文件能力探测，分别检查 COPY 和 MOVE；COPY 不支持时在真实复制前返回冲突，不会执行到一半才发现挂载驱动不支持服务器端复制。
- 普通上传新增持久直接传输会话：正式文件直接PUT到目标路径，不创建`.bfb-staging`，不依赖WebDAV MOVE；队列提供“重新确认”和“继续上传”两种人工恢复动作。
- 普通备份遇到真正无法自动判定的远端冲突时，会把本地完整分P组直接PUT到稳定的`_conflicts/upload-<任务ID>`候选目录；正式旧路径不移动、不覆盖、不删除，问题中心提供“保留现有归档 / 采用新候选”两种选择。
- 画质升级和远程重命名新增独立的 WebDAV COPY/MOVE 能力探测；优先使用 MOVE，只有明确不支持 MOVE 且 COPY 已通过目标大小校验时才使用 COPY+DELETE，普通新上传仍不依赖服务器端移动。

### 修复

- 修复恢复问题分类过于保守的问题：远端暂时不可见、网络抖动和可重试的上传后端错误进入后台低频复核，不再占用人工待处理角标；权限、不支持的方法和无法安全分类的错误仍保留人工处理入口。
- 修复 B 站授权刷新把临时网络失败与 refreshToken 失效混为一谈的问题。网络失败按1小时、6小时、24小时退避后台重试；明确401/403或refreshToken失效停止自动尝试并提示重新登录；未知授权响应只做有限次重试，避免无限请求。
- 明确“完整旧档 + 新候选”仍必须人工选择。系统不会自动覆盖正式旧路径、删除旧文件或混合新旧分P；“保留现有归档”和“采用新候选”都会在执行前再次核验。

- 修复已完成上传但应用在清理事件前重启后，本地视频目录长期残留的问题；启动现在按SQLite分页补偿检查`verified/partial_verified`归档，排除活动下载、上传、确认、画质和传输会话，并在清理前重新核验本地清单、普通文件类型、大小及远端证明。远端不可见、大小冲突、证明缺失、清单异常或目录越界时保留本地文件，不扫描整个`temp`目录、不计算哈希、不下载视频、不删除远端文件。
- 修复本地清理可能把未确认输出一并删除的问题；清理函数现在只接收已核验的清单路径，清单外文件始终保留，部分确认时保留并更新清单以便后续恢复。远端临时不可见按1分钟、10分钟、1小时退避重试，重启后重新由SQLite候选查询补偿。

- 修复 AList/OpenList 对含单引号、百分号、Unicode 组合字符等文件名返回路径型404时，BFB把已上传文件误判为不可见并重复进入补传的问题。普通文件继续走逐文件`stat`快路径；仅在高风险文件名或已有PUT/归档证明的确认阶段按父目录执行一次`PROPFIND`，用文件名、类型和大小唯一匹配，不计算哈希、不下载远端视频、不修改远端文件名。歧义候选和大小冲突继续进入安全人工处理。
- 修复本地待补传文件丢失或大小变化后只能依赖人工修改SQLite任务的问题；新流程会先只读检查持久上传会话中的每个正式远端文件，同大小则自动收尾，全缺失才重建下载，且用持久恢复次数阻止自动循环。
- 修复手动“立即重新检查”可能沿用后台自动恢复语义并触发重新下载的问题；该操作现在严格只读。用户操作与后台复核重叠时会共享同一次远端检查，避免重复请求和基于过期结果重建下载。
- 修复大量未到期或无需自动处理的问题排在队首时，后续已到期事项可能长期得不到后台复核的问题；到期条件和25项上限现在直接在SQLite查询中执行。旧版上传会话的暂存、提升和分阶段确认状态也可被新下载安全标记为已废弃。
- 修复上传流程为兼容不支持MOVE的AList/OpenList驱动而依赖服务器端移动的问题；同名异大小远端文件现在在整批PUT前报告冲突并保留本地文件，不自动覆盖或移动旧归档。
- 修复直传预检与PUT之间的远端竞态：普通上传现在使用条件PUT；只有持久Session存在本次PUT证明时才确认同大小目标，未知同大小或异大小目标都不会被冒认成本次上传结果。
- 修复PUT已经接受但远端暂时不可见时可能使用超大定时器时间戳的问题；人工等待使用独立的SQLite `manual_wait` 状态，不再触发Node定时器溢出警告。
- 修复部分 WebDAV 驱动 PUT 已经写入文件但返回405时，BFB直接把上传判定为失败的问题。现在会复核精确远端路径的类型和大小；仅完全一致时确认成功，文件不存在或大小冲突仍保留失败保护。
- 修复历史收藏关系把缺失顺序映射为超大JavaScript整数后可能造成游标精度损失、跨页重复或遗漏的问题；新版游标显式区分顺序是否已知，旧游标仅在安全整数范围内兼容，否则要求客户端重置列表。
- 修复同UID账号在仅移除登录或远端清理准备阶段崩溃后重新登录，仍可能保留脱离关系、归档快照或暂停任务的问题；启动和数据库重载会在没有未完成删除操作时恢复当前登录账号的归档状态。
- 修复归档目录、列表、账号删除进度和来源删除启动的迟到响应可能覆盖新打开页面或详情的问题；请求现在按归档库会话、账号、操作和详情代次隔离。
- 修复已完成上传会话在远端文件被移除后仍按旧逐文件状态跳过PUT的问题；相同去重键的新上传会原地重开会话并重建文件状态，不创建重复会话。
- 修复上传会话重开时删除旧逐文件明细的问题；现在用持久化代际区分每次上传尝试，旧尝试保留作审计，新任务不能覆盖新代际。
- 修复多分P上传为每个文件重复创建远端确认任务的问题；带持久会话的确认现在按会话单任务恢复，确认任务始终只读，不会消费一次性人工重传权限。
- 修复多分P错峰上传共用第一条文件确认期限的问题；每个分P现在从自己的PUT接受时间计算最长10分钟窗口，等待下一次确认只延后任务，不消耗普通重试次数。
- 修复确定性的远端大小冲突进入普通重试队列、缺少明确恢复入口的问题；409现在进入人工恢复状态，保留本地文件，并防止后续普通同步覆盖恢复提示。
- 修复确认阶段的远端409未统一进入人工恢复的问题；现在会定位冲突分P、结束当前确认任务并创建带会话代际的“重新确认 / 继续上传”入口，避免确认任务误触发普通重试。
- 修复普通同步可能把已有完整旧归档自动换成新版的问题：旧证明与远端重新核验一致时直接保留旧档并跳过新版上传；旧证明不完整、已变化或目标来源不明时整组进入独立候选，不再自动搬入`_history`。
- 修复冲突候选选择缺少并发互斥及旧证明持久留痕的问题；同一候选只能串行处理，采用候选后正式旧文件仍保留，旧路径、大小和媒体元数据继续保存在候选审计记录中。
- 修复远端替换响应丢失或COPY+DELETE中途失败后的恢复问题：MOVE会复核源消失和目标大小；COPY目标校验后先持久化本次操作证明，再删除源文件。画质升级重试时只有携带该证明才会复用既有目标，不会仅凭同大小文件继续删除源文件。
- 收紧远端替换幂等判断：源文件已消失时只有目标大小匹配已知文件证明才视为完成；源仍存在且目标已存在时还必须具有本次操作的持久目标证明，避免无关同名文件被误认成COPY结果。
- 修复人工等待的上传任务未被维护锁识别的问题；`manual_wait`现在与待执行、重试、租约和运行状态一致阻止路径迁移、归档删除及迁移导入，避免任务状态和本地缓存关联被维护操作拆开。
- 修复“继续上传”的一次性授权在任务排队或预检阶段提前消耗的问题；授权现在按文件保存，并只在该文件即将发起PUT时通过SQLite事务原子消费，其他分P和只读确认不受影响。

### 性能与体验

- 已验证本地清理使用SQLite键集分页和按BVID任务/传输会话索引；启动不会枚举`StateManager`全量对象，也不会扫描整个`/app/temp`，每次只处理最多25个候选并串行执行远端轻量核验。

- 上传、后台对账、远端删除预检和播放器特殊路径回退共用短生命周期目录解析缓存；同一父目录的多个分P只发一次目录请求，普通文件和普通播放不增加远端请求，也不保存长期路径别名。
- 普通上传和重启恢复共用SQLite持久传输会话；有本次PUT证明的同大小正式文件可直接确认，未知同大小文件进入安全冲突流程，PUT已接受但不可见的文件只做轻量确认，不重复读取或传输视频正文。
- 队列与问题中心只查询人工等待上传和失败的画质任务，不再每秒读取并反序列化大量无关持久任务；后台自动复核只领取当前已到期事项。
- SQLite升级到schema 8，为全局和账号归档范围建立持久聚合投影，并在关系、远端证明和删除状态变化时按脏BV增量刷新；收藏夹目录继续直接使用实时关系和B站顺序。
- 归档库当前页的来源、删除状态和远端统计改为批量CTE水合，避免为每个卡片重复执行关联子查询。
- 归档库播放器移除全队列窗口排名，焦点定位只统计排序键之前的可播放项，相邻页使用双向键集游标；旧客户端数字页仍保持兼容。
- SQLite实际升级时记录源版本、目标版本、开始、完成或失败以及总耗时；普通启动不增加日志，失败摘要不包含数据库路径、备份文件名、SQL正文或业务数据。

### 升级说明

- SQLite升级到schema 10，为上传会话和逐文件状态增加持久化代际；升级不创建远端暂存目录，不会自动移动或删除已有远端文件。已完成会话重开时保留旧尝试明细，新任务只写入新代际；旧任务代际失效时按已处理状态结束，不会覆盖新尝试。
- schema 7数据库首次升级到schema 8前会生成一致性SQLite备份和SHA256摘要，再在同一事务中构建归档投影。大库首次启动会有一次性构建耗时；失败会整次回滚，旧镜像回滚时必须恢复schema 7升级前备份。
- 发布前根生产依赖审计为11项（2项低危、3项中危、6项高危、0项严重），均来自现有上游传递依赖；文档站生产依赖审计为0项，未执行破坏性强制修复。

## [2.4.6] - 2026-08-01

### 新增

- 删除账号增加两步选择：默认仅移除登录并保留归档，也可在查看去重文件数、空间、共享文件和阻塞项后输入`DELETE REMOTE ARCHIVE`，由后台任务安全清理该账号的已追踪远端归档。
- 归档库来源详情支持删除历史关系、停用收藏夹和已移除账号的具体归档来源；当前活动关系保持禁止删除，并新增“已移除账号”目录与“已删除”筛选。
- 新增持久`archive_delete`任务及最小删除审计。清理按物理路径折叠双层证明，支持租约、心跳、三档退避、重启续作、人工重试，以及AList或本地证明变化后的重新预览确认。

### 修复

- 管理弹窗改为统一栈管理层级、背景隔离和焦点恢复，归档来源删除及账号清理的危险确认会稳定显示在父弹窗上方；Tab与Shift+Tab只在顶层可见控件间循环，Escape按播放器抽屉、归档详情和顶层弹窗的实际层级关闭，不再出现确认框被遮挡或焦点落入隐藏区域。
- 归档详情会记录触发卡片和BV号，按钮关闭、Escape及删除完成后使用`preventScroll`恢复焦点；卡片已被删除或刷新时回退到结果区，手机关闭详情不会带动外层页面跳动。
- 归档搜索拆分输入草稿与已生效查询，300ms防抖完成后才原子替换查询上下文；防抖期间点击旧卡片继续使用旧队列，连续搜索、切换目录/筛选/排序和关闭归档库都会取消过期请求及不再匹配的详情。
- 来源删除从预览请求开始锁定操作按钮，确认取消或请求失败后恢复，任务启动后保持禁用并显示进度；通用危险确认改为固定事件处理器的单实例状态机，快速双击不再重复创建预览或启动任务。
- 修复手机归档库被通用弹窗内边距挤出全屏、返回目录后隐藏容器残留横向滚动导致内容偏移，以及封面`span`缺少块级布局造成占位图与标题重叠的问题；横竖屏现在保持完整边界、固定封面比例和稳定返回位置。
- 相同账号或归档来源的有效删除预览现在复用原`previewId`且不延长30分钟期限；AList连接、归档根、来源或文件证明变化时旧预览会立即失效并清除临时明细。到期预览的文件与来源明细会在启动、数据库重载、创建预览前及后台定时清理，轻量父摘要保留24小时后删除，不影响已开始或已完成的删除任务。
- 远端删除改为全量HEAD预检后逐文件执行，404按幂等成功处理，删除响应丢失后可通过复核继续；共享文件只解除目标来源证明，未知文件和非空目录不会递归删除。
- 修复账号移除过程中配置写入或任务退役失败可能留下半完成状态的问题；账号、关系和调度状态现在会一并回滚，同UID重新登录时可恢复仅移除登录的账号归档。
- 账号远端清理新增持久`preparing`阶段：先取得唯一处理权并停止相关任务，再裁剪共享目标和移除登录；并发重复请求复用同一操作，不再因失败回滚把账号重新写回。准备阶段中断后会按账号是否仍存在恢复确认或继续持久清理。
- schema 7以前仅带`account_detached_at`的已移除账号会在启动时补建无凭据占位快照，重新出现在归档库；全量扫描仅在确有删除留痕时开启恢复事务，消除正常记录的逐项空写。
- 归档删除不再对远端目录发送集合`DELETE`，即使目录当前为空也会保留，从结构上避免检查后并发写入的未知文件被目录删除连带移除。
- 配套BBDown fork将普通UGC的APP播放解析迁移到`PlayerUnite`，按配置优先级请求AVC、HEVC和AV1并合并可用视频流；单个编码请求失败不会丢弃其他成功结果，全部无视频流时仍触发现有APP空响应回退。
- APP结果最高仅480P，或低于明确请求的B站画质档位时，BBDown会额外查询一次WEB播放流；只有WEB视频档位更高才合并WEB视频，APP取得的普通、杜比和Hi-Res音频继续保留，WEB查询失败则安全继续已有APP结果。
- PlayerUnite继续尊重交互模式传入的具体`qn`，默认请求才使用127，避免APP交互选择较低档位后重新解析成最高画质。

### 性能与体验

- 新增隔离假API驱动的Playwright浏览器回归，固定覆盖`1280×720`、`390×844`和`844×390`的弹窗层级、焦点循环、删除防重、搜索竞态、手机返回与播放器抽屉；本地发布前可执行`npm run test:ui`，失败时保留截图与trace，不增加Docker Publish工作流耗时。
- 归档库中的账号清理进度改为轮询轻量删除状态接口，正常路径仅在任务完成或失败时重新加载完整导航，状态查询异常时再安全回退刷新；不再每1.5秒重复执行全库导航汇总。
- 删除任务进入执行阶段后不再为每次状态读取扫描持久任务表；共享路径一次批量判断，逐文件完成通过条件更新递增进度，最终来源、远端证明和视频聚合改为批量读取与分块收尾。新增`remote_files(remote_path, ...)`幂等索引，避免按物理路径核对共享证明时扫描远端文件表。
- 归档导航把收藏夹、账号和全局状态合并为一次SQLite查询；具体收藏夹分页利用关系唯一键移除无意义的BVID聚合，首屏列表和状态汇总共用一次候选集。1万个唯一视频、5万条关系的同构夹具中，导航由约233.5ms降至184.9ms，首个全局问题页由约227ms降至107.4ms；后续键集页保持原查询路径。

### 安全与兼容

- SQLite升级到`user_version 7`，新增无凭据账号快照、删除操作、逐文件进度和已删除来源表；schema 1至6升级前继续生成一致性备份和SHA256摘要，JSON兼容状态保持schema 13，迁移包保持schema 3。
- 归档删除与路径迁移、迁移导入、业务状态清理及远端重命名互斥；活动删除期间禁止修改AList连接、归档根和目录结构。日志与API只返回文件名及脱敏摘要，不返回完整远端路径或凭据。
- 每个B站账号增加持久且不进入Cookie的APP设备标识，并通过BBDown私有临时配置传递；标识与Cookie、access token一同从下载输出和调试日志中脱敏。PGC番剧继续使用原有APP播放接口。
- `v2.4.6`镜像固定BBDown fork Release `bfb-2.0.1`、源码提交`fd926373dfe03d68bf84a1ad8a4ffbf402b00988`和Linux x64 ZIP SHA256 `341eef88483d0cd4461031beee5c8de54192f1427e698970e33b81c3ed933074`；`v2.4.5`镜像继续保持`bfb-2.0.0`的历史行为。
- 从`bfb-2.0.0`升级时，WEB模式且运行配置未变化的未完成轨道可以继续续传；APP模式的旧未完成轨道会先隔离再由PlayerUnite重新下载，避免复用可能仅480P的半成品。已验证成品不受影响。
- 发布前根生产依赖审计保持10项（3项低危、3项中危、4项高危、0项严重），均来自既有上游依赖链；文档站生产依赖审计为0项。

## [2.4.5] - 2026-07-29

### 修复

- 修复画质升级暂存上传未携带下载清单元数据，导致替换后的收藏关系和`remote_files.actual_*`缺少BBDown实际档位及ffprobe媒体参数的问题；普通上传与画质升级现在共用同一元数据构造逻辑。
- 画质升级在任何远端暂存上传或MOVE前严格核验输出清单及ffprobe宽高证明；清单缺失、文件不匹配或证明不完整时保留旧远端文件和本地成品，不再用“实际未知”的新版替换已有归档。
- 兼容历史数据中B站图片域名的HTTP封面地址，仅对白名单域名安全升级为HTTPS，并使曾失败的一次性下架封面回填重新执行。

### 新增
- 账号与同步区域新增全屏“归档库”：只读取本地SQLite即可按全局、账号、当前收藏夹和已停用收藏夹浏览归档，支持状态筛选、标题排序、当前目录或跨账号搜索以及移动端两级目录。
- 归档库可播放卡片复用现有播放器，并按打开时的目录、筛选、排序和搜索结果连续播放；同一BV存在多份成品时按实际尺寸、帧率、完整性、分P数和确认时间稳定选择来源，失效后手动重试可重新选择仍有效来源。
- 新下载现在同时保存BBDown实际选中的B站画质档位和ffprobe确认的媒体参数；播放器按“P序号 · B站档位 · 实际画质与尺寸方向 · 实际编码 · 传输方式”展示，不再显示配置中的请求目标。
- 全局设置新增独立的“AList网页访问地址”；配置后，播放器会通过受保护接口提供“在 AList 中查看”入口，不向播放队列返回WebDAV真实路径。
- 播放器为每次播放尝试显示本次实际采用的“网盘直连”或“BFB代理”，状态只在进程内短期保存，不记录临时签名URL。

### 性能与体验
- 归档库使用上下文绑定的键集游标，每批最多读取50个BV后批量加载关系、视频和远端证明；聚合目录按BV去重，不枚举StateManager缓存，也不逐项查询SQLite。为收藏夹顺序和聚合最近顺序增加schema 6内的幂等索引，不提升数据库版本。
- 下架清单改为服务端按“未上传 / 已上传”筛选和键集游标分页，先使用索引选出当前页键，再批量读取最多50条JSON；移除全量窗口排名、每页总数统计及逐项失败查询，两个标签分别缓存已加载页面，继续翻页只追加新节点。
- 启动后以单并发、最短2秒间隔异步补齐下架视频封面；优先关联已经存在的`covers/<BVID>.webp`，正常同步封面任务优先于历史回填，接口不会等待后台下载。
- BBDown实际档位按CID随下载清单、上传任务和远端文件记录传递；旧归档缺少该记录时直接省略B站档位，不根据分辨率猜测。浏览器仍只补齐可确认的实际宽高和时长。

### 安全与兼容
- 下架封面下载限制为B站HTTPS图片域名，重定向逐次复核，并拒绝凭据、私网解析、非图片响应、超过8MB的内容和超过10秒的请求。
- AList网页入口重新校验登录、文件归属、最终确认状态和路径边界；网页地址禁止携带凭据、查询串或片段，自定义基础路径和Unicode文件名按路径段编码。
- 播放代理改为最多5跳的手动重定向校验：跨源仅允许公开HTTPS地址，且认证一旦移除便不会在后续跳回AList时恢复；同时拒绝私网DNS、循环跳转和异常端口。
- SQLite升级到`user_version 6`，为远端文件增加实际媒体参数标量列；升级前继续生成一致性备份和SHA256摘要。JSON兼容状态保持schema 13，迁移包保持schema 3。
- 根生产依赖审计保持10项（3项低危、3项中危、4项高危、0项严重），均来自现有上游依赖链且没有可直接安全应用的完整修复；文档站生产依赖审计为0项。

### 可靠性修复
- 修复归档路径“保留旧目录”和“清理旧目录”并发时可能出现先删除后保留的竞态；清理现在通过SQLite原子领取，处理中断后只恢复为等待人工复核，不会自动继续删除。
- 修复Windows上的`taskkill`可能无限等待并卡住关闭流程；每次调用现在有3秒硬上限，并在失败后执行根进程兜底终止。
- 修复封面ffmpeg或历史回填尚未退出时迁移导入、状态清理可能替换SQLite的问题；封面工作未能在期限内停止时会拒绝操作。
- 修复同一远端文件出现在多个收藏关系时，浏览器补齐的实际画质只更新当前关系的问题；等价已验证记录现在在同一事务内同步，ffprobe证明仍不会被覆盖。
- 修复播放传输状态查询暂时失败时误显示“传输失败”、切换视频后旧媒体上报仍可能完成，以及下架弹窗关闭或快速重开后旧请求仍可能干扰当前页面的问题。
- 修复非标准分辨率被向下归入常见画质档位的问题；实际画质现在统一按真实短边显示，例如`1772×3840`显示为`1772p`并标注竖屏，帧率不低于50时追加`60`。
- 修复播放器把解码失败和网络失败混为一类、对HEVC错误无意义切换BFB代理的问题；现在依据媒体错误码、实际编码、请求编码和浏览器HEVC能力分流，网络错误最多代理一次，解码错误直接提示兼容性。
- 修复最终播放错误仍停留在“检测传输中”或把状态查询失败写成传输失败的问题；已确认的直连或代理状态会保留，无法确认时显示“传输方式未知”，旧播放请求不能覆盖新视频状态。
- 修正BBDown选择流日志的字段识别：只接受正文行首的`[视频]`标记，按真实`[画质][编码][码率][预估大小]`格式解析，同时兼容历史`[画质][分辨率][编码]`格式，避免标题或错误文本误写B站档位。

### 已知事项

- APP接口在部分账号或视频上可能只返回480P播放流，但BBDown仍能完成下载，当前流程会将该结果视为成功。关闭Hi-Res和杜比时暂建议使用WEB接口；APP结果识别与安全自动回退计划在下一版本修复，本轮保留现有APP模式和下载逻辑。

## [2.4.4] - 2026-07-26

### 新增
- 收藏夹详情新增归档播放器：已验证或部分验证的关系可直接播放远端已确认的MP4、M4V和WebM，按收藏夹及分P顺序连续播放，并支持进度恢复、倍速、全屏、网页全屏和画中画。
- 新增分页播放队列与受保护的文件流接口；历史关系可单独播放，不会混入当前收藏夹的连续队列。
- 播放器队列增加全收藏夹搜索，可按归档标题、UP主和BV号定位可播放视频；搜索结果保留原收藏顺序位置，选择后回到完整队列继续播放。
- 归档播放器增加“优先网盘直连 / 始终BFB代理”设置；默认在登录和文件归属校验后透传合格的外部HTTPS 302，无法安全直连时自动使用现有流式代理。

### 修复
- 修复已同步收藏夹的“全部”详情依赖B站实时列表，导致已上传后失效的视频缺少原始标题、封面和准确备份状态的问题；已同步收藏夹现在统一从SQLite分页读取，打开和滚动详情不再额外请求B站。
- 未同步收藏夹继续支持B站实时浏览，但已知视频会使用归档前元数据、本地封面、关系状态和充电限制信息补全；`/detail-items`与兼容的`/state-items`共用同一处理逻辑。
- 收藏夹索引完整度只按当前活动关系与B站总数比较，历史归档不再误增已索引数量；当前关系按收藏顺序显示，历史关系按最后发现时间倒序排列。

### 性能与体验
- 详情页补充本地索引/实时来源、最近同步时间、当前与历史记录数量，并为非活动关系显示“历史记录”次级徽章。
- 完整区分上传确认中、部分备份、已备份、待补传、充电视频、下载失败和已上传且失效；远程封面失败时按视频状态回退本地封面。
- 详情分页失败会保留已加载内容并提供原位重试；切换筛选或关闭弹窗会取消旧请求，避免迟到响应覆盖当前列表。
- 播放器采用石墨黑主从工作面，桌面端显示编号队列，移动端适配390px竖屏和844×390横屏；竖屏视频完整居中显示且不裁切。
- 收藏夹封面的播放提示改为仅在桌面鼠标悬停或键盘聚焦时出现，窄屏和触屏设备不再常驻显示三角遮罩。
- 播放队列同页切换只更新当前项，不再重建整列或滚动页面；手机切换分P、上下视频、自动连播及跨页时保持当前视角，桌面右栏仅在当前项超出可视区域时滚动自身。
- 修复关闭播放器后重开时队列容器沿用旧视图标记、导致当前50项未重新渲染的问题。
- 桌面播放队列改为轻量轨道列表，以细左标和柔和背景表示当前项，并在切换后保留队列按钮焦点和无障碍当前状态。
- 播放队列改为每批50项的双向无限浏览，并增加72×45封面缩略图、本地封面到原始封面的回退、搜索防抖和原位失败重试；移动端加载与定位不滚动播放器外层视角。
- 手机竖屏默认使用全视口沉浸播放器，上下滑动沿现有分P和收藏夹顺序切换；完整队列与搜索收进底部抽屉，横屏自动恢复原布局，用户可按浏览器保存开关选择。

### 安全与接口
- 管理员登录改用独立SQLite持久Session：普通会话设置24小时服务端上限，登录页可主动选择固定保持30天，Docker更新或容器重启后无需强制重新登录。
- 浏览器只保存签名后的随机会话ID，SQLite使用HMAC会话键且不保存管理员密码；登录重新生成会话、退出双端撤销，管理员凭据或`SESSION_SECRET`变化会使旧会话失效。
- 会话库不进入迁移包和导入前自动备份，不启用滚动续期，队列每秒轮询不会产生Session写入；启动时清理结构损坏、主体或到期字段不一致的记录，会话数据库及WAL侧文件在支持的平台上收紧为`0600`。
- 播放文件必须属于当前登录用户、指定收藏夹和已验证关系；BFB仅转发单段Range和有限响应头，客户端中断会终止上游请求，不返回AList真实路径、地址、认证头或响应正文。
- 已知视频扩展名强制使用媒体MIME，避免错误的远端HTML响应以同源页面类型执行；多段Range、路径记录变化和未确认文件均拒绝读取。
- 302直连只接受不含凭据、非AList同源且非本机或私网地址的绝对HTTPS目标，并禁止缓存和发送来源页；临时签名地址不会写入日志或持久状态。
- 非HEVC直连播放失败时只自动回退代理一次并恢复进度，避免签名过期造成中断和失败循环；已知HEVC解码失败不重复传输同一文件。

### 依赖维护
- 新增固定依赖`artplayer@5.4.0`，仅从应用内精确资源路由按需加载，不使用CDN或公开整个依赖目录。
- 将`archiver`从7.0.1升级到8.0.0并同步类型定义，按原生ESM的`ZipArchive`接口继续生成流式ZIP64迁移包。
- 移除Archiver旧版携带的`archiver-utils`、旧`glob`和旧`readdir-glob`依赖链；当前根生产依赖完整审计为10项（3项低危、3项中危、4项高危），剩余告警继续等待B站API、Express和WebDAV上游修复。

### 升级说明
- SQLite继续使用`user_version 5`，JSON兼容状态继续使用schema 13，迁移包继续使用schema 3；本次升级不修改业务数据库和迁移包格式。
- `v2.4.3`及更早版本的管理员Session只保存在进程内存中，更新容器后需要重新登录一次；之后可在登录页选择固定保持30天。
- 新会话库位于持久化的`data/auth-sessions.sqlite`，不进入迁移包或导入前自动备份；修改管理员账号、密码或`SESSION_SECRET`会使已有会话失效。

### 已知边界
- 播放器不执行转码，HEVC等编码能否播放取决于浏览器和设备的原生解码能力。
- 网盘直连使用临时签名URL以减少BFB服务器流量，该地址会出现在当前浏览器的网络面板中；AList地址和认证凭据不会下发。

## [2.4.3] - 2026-07-22

### 新增
- 新增“归档路径迁移”：同一AList挂载存储内通过扫描预览、WebDAV COPY、延迟可见确认和SQLite事务切换，把整个旧归档树安全复制到新路径。
- 路径迁移保存持久进度、冲突、目标额外内容和失败摘要；支持暂停、恢复、切换前取消、保留旧目录和二次确认清理旧目录。

### 可靠性修复
- 有远端证明或路径任务时，直接修改`alistDest`返回`PATH_MIGRATION_REQUIRED`，不再让新旧路径在后续任务中混用。
- 迁移期间停止领取同步、下载、上传、远端确认和画质任务；目标同名同大小文件可复用，大小或类型冲突会在复制前阻断。
- 修复路径迁移预览取消竞态、可复用目标的开始前复核和远端异常路径段校验；失败重试等待直接使用持久化的最早时间，避免退避期间每秒轮询SQLite。
- 修复延迟可见目标被重复COPY、进程重启后重复COPY和缺少目标根目录时无法递归建目录的问题；COPY后的核验时间持久化，24小时不可见会暂停迁移。
- 路径切换只按字面前缀更新`remote_files`，同时覆盖历史冲突归档中的`archivedPath`，避免通配符误匹配或历史证明残留旧路径。
- SQLite升级到`user_version 5`，迁移清单以逐项记录保存，不把完整远端文件清单放入内存；应用重启后按阶段恢复。
- 迁移导入只按实际恢复的组件重置启动检查：恢复SQLite状态时重新检查旧画质任务，完整包恢复`temp`时重新检查旧缓存，纯配置、账号、封面或日志导入不会触发无关扫描。
- 旧画质任务缺少BVID、候选数变化或事务写入失败时不写完成标记；无法关联活动收藏关系的旧缓存保留在清理预览中，不自动删除，也不会在每次启动重复扫描。

### 性能优化
- 删除仅由测试保留的同步下载缓存恢复检查器，运行时与测试统一使用现有异步检查器，缓存占用、可续传内容和残片分类只保留一套实现。
- 旧格式`quality_download`任务使用`schema_meta`完成标记只迁移一次；后续启动不再查询画质任务，首次迁移只读取旧格式候选，并在10万项安全上限内通过单个事务完成合并和标记写入。
- 无有效清单的旧式`temp/BV*`缓存改为异步顶层检查并写入一次性完成标记；完成后的启动不再读取目录，检查期间仅暂缓新下载，不影响上传、远端确认和队列查询。

### 升级说明
- SQLite schema 1至4会在启动前生成一致性备份和SHA256摘要，再自动升级到`user_version 5`；旧镜像不能直接打开schema 5数据库，回滚时应恢复升级前备份或使用兼容JSON导出。
- JSON兼容状态继续使用schema 13，迁移包继续使用schema 3，不需要重新生成已有迁移包。
- 升级不会自动移动现有AList归档；只有用户在设置页完成扫描、复制和确认后才会切换`alistDest`，旧目录默认保留。

### 已知事项
- 当前`npm audit --omit=dev`报告15项上游依赖风险，涉及`axios`、`body-parser`、`brace-expansion`及`fast-xml-parser`；其中部分问题暂无修复。本轮不执行破坏性`npm audit fix --force`。
- 本轮只使用模拟WebDAV回归测试；AList `v3.61.0`下天翼驱动的真实COPY延迟和目录DELETE行为仍需部署阶段验证。迁移切换进入`cleanup_pending`后可生成迁移包，但旧目录仍需单独确认保留或清理。

## [2.4.2] - 2026-07-15

### 文档与界面
- 文档站改为Photo风格的产品介绍首页，使用全局编号侧栏，并统一文档站、应用页面和浏览器页签的绿色电视图标。

### 性能优化
- 队列看板继续每秒刷新任务状态，但`/api/queue/state`不再同步递归扫描`temp`；缓存占用、可续传会话和待清理残片改为一次异步遍历并共享10秒快照，页面隐藏时暂停轮询。
- SQLite升级到`user_version 4`，充电限制和远端核验调度改用标量列及索引查询，不再在运行时扫描对应JSON字段。

### 升级说明
- schema 1–3数据库会自动升级到schema 4；升级前在`data/backups`生成一致性SQLite备份和SHA256。旧镜像不能直接打开schema 4，回滚时应恢复该备份或先导出旧版JSON。

## [2.4.1] - 2026-07-14

### 新增
- 新增“充电视频”状态：详情接口确认当前账号无完整权限后不再启动BBDown，默认每`7天 ± 12小时`检查全部启用账号；临时接口异常改为约6小时后复查，账号重新登录或启用会立即唤醒。
- 数据迁移包升级到schema 3，提供轻量与完整两种模式；完整模式包含`temp`下载会话、aria2断点、待补传文件和历史分P，导出前显示规模并在运行任务存在时拒绝切换。
- Debug日志默认保留14天、最多200个文件和256MiB，支持环境变量覆盖并在启动及每次写入后安全轮转。
- 登录页和主页面显示应用版本、构建分支与提交短哈希；版本链接指向对应GitHub提交，另提供项目主页入口。

### 可靠性修复
- 同名异大小的远端文件会先整组移动到`_history/<时间>`再上传当前版本；MOVE或后续PUT失败时保留本地文件与真实远端记录，重试不会重复归档。
- AList上传间隔只限制真实PUT；单任务已完成部分文件后出现405时按临时上传会话异常延后重试，首文件直接405仍按确定性驱动错误处理。
- 修正无效下载残片统计与清理：`_invalid`、无有效分P且不可续传的失败会话不再计为可续传，历史残片仍只进入手动清理预览。
- 远端重命名使用归档时记录的发布时间、CID、分P序号、画质和编码；资料不足时跳过，交换与循环命名使用临时路径，部分MOVE失败会探测真实位置并尽量回滚。
- 画质重调区分明确不同、相同、未知和进行中；未知项目必须显式强制提交，旧文件DELETE失败会无限持久重试并保留新版成品。
- 删除账号会终止依赖该凭据的任务并保留下载会话、待补传文件和远端证明；存在其他启用账号时自动改派，同UID重新登录后恢复关联任务。
- 启动时孤立`upload_failed`记录通过SQLite游标分页写回持久任务，损坏清单或无可用账号不会形成立即重试循环。

### 性能优化
- SQLite `user_version`升级到3，收藏夹分页、失效列表、远端确认、失败恢复和统计改为索引查询；视频与关系运行时缓存各自限制为256条。
- 启动状态规范化使用版本标记和每批500条事务；远端确认、失败候选、失败记录及冷却状态改为分页或单行CRUD，不再加载或重写完整集合。
- “启动恢复每批数量”更名为“任务预取上限”；持久任务可以完整保存在SQLite中，内存队列只领取预取上限内的任务。
- 同一BVID、相同画质档案的画质重调只下载一次，再分别向多个收藏夹目标执行上传、替换和清理；不同画质及失败目标保持隔离。

### 安全与迁移
- BBDown临时凭据移到系统私有临时目录，目录和文件分别使用`0700`与`0600`权限；启动和正常关闭时清理遗留目录，迁移包不会导出或恢复凭据。
- 迁移导出与导入改为ZIP64流式处理，校验路径、CRC/SHA256、文件数、总展开大小和压缩比；配置、账号、日志及SQLite全部校验并reload成功后才提交切换，失败时原子回滚。
- 日志持久化、实时广播、SSE和队列错误统一使用同一份脱敏对象，移除Authorization、Cookie、Token、密码、URL凭据及敏感查询参数；关闭前强制flush。
- `/api/login`按客户端IP限制15分钟内最多5次失败，成功登录不计数；默认密码、默认会话密钥、Secure Cookie和Cookie导出状态会输出不含实际值的启动警告。
- 删除无前端调用的旧状态、缓存清理、远端浏览及旧重命名接口；新重命名入口会重新校验路径范围、源文件和目标冲突。

### Docker与依赖
- 内置Compose中的AList固定升级到`v3.61.0`；外接AList不会自动升级。
- `@renmu/bili-api`升级到`2.14.0`、`tsx`升级到`4.23.0`，新增固定的登录限速和流式迁移依赖。
- Docker与GitHub Actions注入构建分支和完整提交号，镜像启动日志与页面可以追溯到对应源码。

### 升级说明
- SQLite会自动迁移到`user_version 3`，状态兼容schema自动升级到13；首次启动会为尚未备份且未分类的旧永久失败视频生成一次轻量权限分类任务。
- 旧配置中的`startupRecoveryBatchSize`自动迁移为`queuePrefetchLimit`，无需手工修改`config.json`。
- 迁移包schema 3继续支持导入schema 1/2旧包；完整导入要求目标`temp`为空，不会静默覆盖现有下载会话或缓存。
- 必须继续持久化挂载`data:/app/data`和`temp:/app/temp`；升级或回滚前应备份SQLite、WAL、SHM及本地缓存。
- 内置Compose使用AList `v3.61.0`，升级其数据前应先备份AList配置与数据库，并验证WebDAV PUT、MOVE、列出、下载和删除；当前真实服务器仍运行`v3.60.0`，`v3.61.0`真实网盘驱动兼容性尚未验证。
- 源码运行继续要求Node.js 24；BBDown、aria2、FFmpeg及远端目录结构没有变化。

### 已知事项
- `npm audit --omit=dev`仍报告`@renmu/bili-api -> fast-xml-parser`的2个中危告警；自动修复会破坏性降级主依赖，本版本未执行`npm audit fix --force`。

## [2.4.0] - 2026-07-12

### 重大更新
- 运行状态与任务队列迁移到固定依赖 `better-sqlite3@12.11.1` 的 `data/bfb.sqlite`，使用 WAL、外键、事务迁移和完整性校验；旧 `state.json` 自动导入并永久归档原文件、SHA256与迁移摘要。
- 下载、上传、远端确认、历史上传和画质重调统一使用SQLite持久任务；支持唯一去重、优先级、`not_before`、任务租约和崩溃恢复，内存中只领取队列高水位以内的任务。
- BBDown升级为固定fork Release `bfb-2.0.0`，下载统一启用aria2；下载会话按账号、配置和分P CID保存进度，复用已验证成品，并将被替换或删除的旧分P归档到远端 `_history`。

### 新增
- AList WebDAV上传显式发送准确 `Content-Length`、MIME和文件时间头，拒绝空文件与空目录；上传前同名同大小直接确认，上传后通过异步任务完成两阶段远端校验。
- 新增上传故障分类、指数退避、自动熔断和下载背压；认证、驱动或持续网络异常会暂停新下载，上传恢复后自动继续，单个确定性失败不会阻塞其他任务。
- 全局设置新增“网页接口 / APP接口”；网页接口触发 `v_voucher` 后冷却3分钟并单任务探测，APP缺少播放信息时仅当前视频回退网页一次。
- 数据迁移包升级为schema 2，包含SQLite一致性备份、可读JSON快照、下架索引和封面；旧schema 1包仍可导入，并提供 `npm run state:export-json` 回滚导出命令。
- 队列与详情状态新增“待补传”“已上传·确认中”“部分备份”，缓存横条展示可续传和待清理数据，异常时显示上传后端或B站风控状态。

### 修复与优化
- 修复部分AList驱动因缺少文件大小返回405、上传成功后延迟可见被误判失败、远端目录异常被静默忽略等问题。
- 四字节Emoji文件名优先使用原名，仅在确定性不兼容错误时改用兼容名并避免远端重名冲突。
- 上传失败、多目标部分成功和历史文件未确认时保留本地文件；上传器只处理会话白名单成品，不上传Debug、aria2控制文件或原始轨道。
- 修复启动缓存扫描与持久任务派发竞态、`stop → start`后到期任务不恢复、任务唤醒误停租约心跳以及缓存满时零延迟空转的问题。
- 下载中断会保留可恢复轨道；仅在416、资源长度变化或控制文件损坏时隔离当前轨道，混流失败保留原始音视频并重新生成成品。
- 收到SIGINT/SIGTERM后停止领取任务，终止BBDown/aria2进程树并等待控制文件落盘，随后释放租约并关闭SQLite。

### Docker / 发布
- 应用、测试和Docker运行时统一升级到Node.js 24；GitHub Actions固定 `ubuntu-24.04`，在测试和TypeScript构建通过后才发布镜像。
- Docker固定下载BBDown fork与SHA256，不再安装.NET SDK或构建时跟随上游master。
- FFmpeg固定为BtbN `n8.1.2-22-g94138f6973-20260711` Linux x64 LGPL构建；运行镜像只通过apt安装aria2和证书，减少完整动态依赖。
- Actions升级到Node 24版本，移除仅构建amd64时无用的QEMU步骤，并启用Buildx GHA缓存。

### 升级说明
- 首次启动v2.4.0会自动把旧 `data/state.json` 迁移到SQLite；迁移失败时不会启动调度，也不会删除原JSON。
- 必须继续持久化挂载 `data:/app/data` 和 `temp:/app/temp`；不要在程序运行时手动删除SQLite、WAL或SHM文件。
- 源码运行要求Node.js 24；Docker用户无需在宿主机安装Node.js、BBDown、aria2或FFmpeg。
- 如需回滚到读取 `state.json` 的旧镜像，应先停止当前程序并执行 `npm run state:export-json -- data/bfb.sqlite data/state.rollback.json`。

### 已知事项
- `npm audit --omit=dev`仍报告 `@renmu/bili-api -> fast-xml-parser` 的2个中危告警；自动修复会破坏性降级主依赖，本版本未强制处理。
- 收藏夹扫描的部分普通异常仍可能把完整Axios错误对象写入原始服务端日志，其中可能包含请求上下文；排障日志请勿公开分享。

## [2.3.3] - 2026-06-28

### 新增
- 新增数据迁移功能，可导出/导入配置、账号、备份状态、下架记录、日志和本地封面缓存，导入前会自动备份当前 `data`。
- 已失效视频会保留历史标题、UP 主、简介和半尺寸 WebP 封面缓存；本人可见的自投稿失效视频会尝试回填真实信息并继续纳入备份流程。

### 优化
- 队列看板的本地缓存状态改为横条展示，下面保持“待下载 / 下载中 / 待上传 / 上传中”四列。
- 修复 Docker 跨挂载场景下封面缓存写入失败的问题。
- 优化收藏夹详情和下架清单的长标题布局，避免状态徽章被挤出。

## [2.3.2] - 2026-06-05

### 优化
- Web 面板保留原有大布局和文字颜色，统一浅色面板、按钮、帮助入口、弹窗和移动端窄屏下的交互状态。
- 问号帮助按钮补充 hover / focus 状态和 `aria-label`，提升键盘与辅助技术可用性。
- 前端抽出隐藏和状态提示 helper，减少脚本中直接操作 `style.display` / `style.color` 的临时样式逻辑。
- 清理数据、旧命名文件、画质重调等弹窗的隐藏区块改为 class 控制，展示状态更一致。
- Docker 发布工作流新增 `dev` 分支镜像标签，`dev` 分支发布 `minori0721/bili-favorites-backup:dev`，`latest` 仍只由 `main` 发布。

### 安全
- 删除账号、导出 Cookie、全量扫描并对账、远端重命名、画质重调统一改为站内确认弹窗，替换浏览器原生 `confirm()` / `prompt()`。
- Cookie 导出必须输入 `EXPORT_COOKIE` 才能继续；浏览器阻止自动复制时会给出明确提示。
- 远端重命名和画质重调在预览后再次确认，确认弹窗明确说明 AList 文件名修改、重新下载上传和旧远端文件删除条件。

## [2.3.1] - 2026-06-01

### 新增
- 新增本地缓存软上限设置，默认 10GB；当 `temp` 用量达到上限时只暂停启动新的下载任务，待下载任务继续留在队列中，上传与上传后清理继续运行。
- 队列看板新增本地缓存用量与下载暂停提示，缓存清理或上传清理后会自动唤醒下载队列。

## [2.3.0] - 2026-05-15

### 修复
- 修复收藏夹详情弹窗筛选按钮过宽时出现横向滚动条的问题。
- 修复队列看板只显示裸耗时、容易被误解为剩余时间的问题，现在会显示“等待 / 运行 / 上传”含义。
- 修复旧命名文件重命名后未同步本地远端文件记录，可能导致后续 AList 对账误判缺失的问题。
- 修复同一 BV 同时存在于多个收藏夹时，共享下载目录被提前清理导致后续收藏夹目录漏传的问题。
- 修复上传没有产出远端文件记录时仍可能被误标记为已上传/已验证的问题。
- 修复 AList 目录读取失败被当成空目录，进而误判远端文件缺失的问题。
- 修复已有旧文件名模板缺少 `<bvid>` 时仍可能继续产生同名冲突的问题。
- 修复命令行登录仍使用 Web 扫码，与 Web 面板 TV 登录不一致的问题。
- 修复队列 active 状态在重启或内存队列丢失后无法重新入队，导致视频长期卡在未上传的问题。
- 修复全量扫描过程中提前标记完成，导致收藏夹详情误提示“继续扫描后补齐”的问题。

### 优化
- “一键重命名网盘文件”改为“检查旧命名文件”，新增预览、勾选、二次确认、执行结果展示与跳过原因说明。
- 新增 AList 旧命名文件递归扫描与完整路径重命名接口，只允许在当前目标路径下同目录改名，并校验目标冲突。
- 下载任务新增低速 watchdog：运行超过 30 分钟且最近 10 分钟平均增长低于 10KB/s 时自动终止并进入现有重试流程。
- 全局设置补充生效范围说明，明确轮询/并发立即生效，画质、命名模板、AList 路径等只影响新任务。
- 账号与同步区域新增“立即同步 / 状态对账 / 全量扫描并对账”帮助弹窗，支持简要和详细介绍。
- 全局设置标题新增可视化执行流程弹窗，按当前表单值展示轮询、下载、上传、重试与 AList 对账行为。
- 下载结果会在所有目标上传完成后再统一清理，支持同一视频复用到多个收藏夹目录。
- 全量扫描完成后会把本轮未再出现的收藏夹关系标记为非当前收藏，改善取消收藏后的状态展示。
- 默认文件名模板改为 `<videoTitle>-<bvid>`，并对旧配置做加载时迁移。
- 后端配置保存增加字段白名单和范围校验，减少异常配置导致任务行为不可控。
- BBDown 画质与音频优先级映射调整：Hi-Res / 杜比走 `--encoding-priority`，清晰度走 `--dfn-priority`。
- 任务队列提高并发数后会立即补满可用并发槽。
- 画质重调下载阶段接入原有下载队列，上传替换阶段接入原有上传队列，分别受当前下载/上传并发控制。
- 下载、上传和画质重调任务等待自动重试时继续显示在队列看板中，并展示等待重试倒计时。
- 队列看板纳入画质重调任务与调度状态，按原有“待下载 / 下载中 / 待上传 / 上传中”展示重调下载、上传替换、删除清理和同步/对账排队详情。
- B 站风控/登录异常识别覆盖更多 HTTP 状态码、API code 与中文错误文本。
- 收藏夹索引提示改为区分“扫描未完成”和“B 站报告总数但接口未返回明细”，避免全量完成后继续误导用户补扫。
- 账号卡片新增授权健康状态，明确展示自动刷新是否可用、最近刷新成功时间、失败原因和是否需要重新扫码。
- 旧命名文件重命名改为前端分批提交，避免大量项目一次提交触发请求体过大。
- 画质重调任务改为前端每批 50 项提交，避免大量勾选后触发后端单批安全限制。
- 画质重调会记录新上传文件的目标画质设置，后续检查可升级画质时会跳过已符合当前设置的远端记录；旧记录仅在文件名模板可可靠判断时跳过。
- 新增“检查可升级画质”入口，可按当前画质/编码/Hi-Res/杜比设置预览、勾选并后台重调画质；新版文件会先上传到临时目录并验证，再删除旧远端文件并移动到正式目录。
- AList 对账参数上限提高：并发和限速最高可设为 100，每轮最多补传数量最高可设为 1000。
- 新增“清理数据”入口，可勾选清理页面缓存、临时文件、日志、Debug 日志、备份状态、账号和配置；重要数据要求二次确认，同步/扫描/对账或下载/上传运行中会拦截关键清理。
- Docker Compose 中 app 与 AList 服务改为 `restart: unless-stopped`，异常退出或宿主机重启后会自动拉起。

### 安全
- JSON 配置/状态读取失败时不再静默重置默认值，会保留 `.corrupt-*` 备份并报错。
- BBDown Cookie / APP access token 不再通过命令行参数传入，改为每次下载使用临时配置文件，并对输出与 debug 日志脱敏。
- 登录成功后重新生成 session，降低 session fixation 风险。
- `/api` 下非 GET 请求增加同源校验。
- Cookie 导出改为 POST + `EXPORT_COOKIE` 手动确认，并支持 `ALLOW_COOKIE_EXPORT=false` 关闭。
- Session Cookie 支持通过 `COOKIE_SECURE=true` 启用 secure 标记。

### Docker / 发布
- GitHub Actions 发布 `latest` 镜像，并支持 `v*.*.*` 标签发布版本镜像。
- Docker Node 基础镜像更新到 `node:20-bookworm-slim`，并保留 `NODE_IMAGE` 构建参数。
- BBDown 下载支持固定 `BBDOWN_VERSION` 和可选 `BBDOWN_SHA256` 校验。
- Compose 示例改用 `please-change-*` 默认值，并将 AList 示例镜像固定到 `xhofe/alist:v3.41.0`。

### 已知事项
- `npm audit --omit=dev` 仍会报告 `@renmu/bili-api` 传递依赖 `fast-xml-parser` 的中危项；自动修复会降级主依赖，本轮未强行处理。
- 默认 `ADMIN_PASS` / `SESSION_SECRET` 只做文档提示，不做启动拒绝。

## [2.2.2] - 2026-05-12

### 修复
- 修复收藏夹详情误用本地状态表导致未索引收藏夹显示为空、数量偏少、标题仅显示 BV、UP 显示 Unknown、封面缺失的问题。

### 优化
- 收藏夹详情“全部”视图改为实时读取 B 站收藏夹分页数据，并叠加本地备份状态。
- 打开详情时会记录当前页视频元数据与收藏顺序，但不会触发下载队列。
- 状态筛选继续基于本地已索引记录，并显示已索引数量提示，避免把局部统计误认为全量结果。

## [2.2.1] - 2026-05-12

### 优化
- 优化任务日志“队列看板”布局，改为固定高度与列内独立滚动，避免大量待下载任务撑乱页面。
- 队列卡片改为两行标题、省略长文本，并用小标签展示重试次数与耗时。
- 队列看板新增基于稳定任务标识的差异更新与移动动画，减少每秒整块重绘带来的闪烁。
- 待下载任务过多时限制首屏渲染数量，并显示剩余任务数量，降低页面压力。

## [2.2.0] - 2026-05-12

### 新增
- 任务日志新增“队列看板”，可按“待下载 / 下载中 / 待上传 / 上传中”查看实时任务流转。
- 新增队列快照接口，用于前端稳定展示当前下载、上传和等待队列状态。
- 收藏夹详情记录并使用 B 站收藏夹页码与页内顺序，列表展示更贴近 B 站真实顺序。

### 优化
- 收藏夹详情中的“未上传”不再包含“未上传并失效”，失效视频单独归类统计。
- BBDown 输出文件名过长时会自动截断视频标题并重试，避免长标题视频反复卡在合并失败。
- 调试日志继续保留解析失败线索，便于区分 B 站解析异常和本地文件输出异常。

### 修复
- 修复收藏夹详情接口读取失败状态时未完全绑定收藏夹关系的问题，减少跨收藏夹状态串扰。
- 修复部分超长标题视频因系统文件名限制导致每轮重复失败的问题。

## [2.1.9] - 2026-05-11

### 修复
- 修复 Docker 构建阶段 `apt-get install` 大量 `403 Forbidden` 导致镜像构建失败的问题。
- Dockerfile 调整为：默认使用 Debian 官方源（CI 稳定），国内环境可通过 `ARG APT_MIRROR=tuna` 显式切换清华镜像。

## [2.1.8] - 2026-05-11

### 优化
- 对账提速：AList 目录列表增加短时缓存，同轮多视频复用目录快照，显著减少重复请求。
- 对账限速增强：新增目录维度的节流分配，并保留全局限速与并发控制，降低高并发时对单目录冲击。
- 调度增强：同步运行中再次点击“立即同步/对账”不再直接失败，改为排队到下一轮执行。

### 新增
- 任务日志新增第三档“调试模式”，可与“精简/原始”切换。
- BBDown 在“解析此分P失败”时会自动补跑一次 `--debug --only-show-info` 并保存调试日志到 `data/debug/*.log`。
- 日志持久化：网页日志写入 `data/logs.json`，容器重启后可继续查看最近日志。

### 新增
- 收藏夹详情新增 5 组状态筛选与计数：
  - 全部
  - 已上传
  - 未上传
  - 未上传并失效
  - 已上传且失效
- 新增接口 `GET /api/users/:id/favorites/:mediaId/state-items`，支持按状态过滤并返回统计汇总。

### 修复
- 修复收藏夹详情中“是否已上传”判断未绑定 `mediaId`，导致跨收藏夹状态串扰的问题。
- 修复失败状态按 `userId+bvid` 记录导致跨收藏夹串扰的问题，改为按 `userId+mediaId+bvid` 记录。
- 禁用旧“全局上传证明自动继承为关系已上传”逻辑，避免 AList 实际缺失但界面仍显示已上传。
- 失败任务会在下一轮优先回队列，不再依赖再次翻页命中才可能重试。

## [2.1.7] - 2026-05-11

### 调整
- `解析此分P失败` 不再作为永久失败处理：本轮任务队列不重试，下一轮同步扫描时会重新入队。
- 明确的下架/不可见/`Arg_KeyNotFound` 仍会停止自动重试，避免无意义请求。

## [2.1.6] - 2026-05-11

### 修复
- 修复 BBDown 输出“解析此分P失败”但程序仍显示“下载完成”的误判；现在必须真实产出媒体文件才会进入上传。
- 修复 v2.1.5 关系级状态迁移后，历史已上传视频被当作新视频重新下载的问题；历史全局上传证明会先导入到收藏夹关系，再等待 AList 状态对账确认。
- 修复关系级远端对账回退使用全局 `remoteFiles` 导致跨收藏夹目录误判已上传的问题。
- 下载/上传队列失败与重试现在会进入网页日志；永久解析失败不会在后续扫描中反复自动入队。

## [2.1.5] - 2026-05-10

### 变更
- 备份状态改为按“账号 + 收藏夹 + BV号”记录，同一个视频出现在多个收藏夹时，会分别确认每个 AList 目录是否存在文件。
- 同一个 BV 可复用一次下载结果，并按需上传到多个收藏夹目录，避免重复下载但不再漏传目录。
- 自动初始化扫描加速：初始化阶段每轮补扫更多历史页，手动同步也会扫更深。

### 修复
- 修复跨收藏夹全局去重导致某些收藏夹目录文件数明显少于 B站收藏夹数量的问题。
- 修复远端对账只看目录是否有文件、未按 BV号匹配文件名导致误判“已上传”的问题。
- 修复热页扫描把已完成初始化的收藏夹重新标记为初始化中的问题。

## [2.1.4] - 2026-05-10

### 变更
- 统一上传状态判定为 `videos.backupStatus`，旧 `processedByUser` 不再参与业务判断。
- 手动/自动对账统一走新远端校验路径，历史记录即使没有 `remoteFiles`、只要有 `remotePath` 也可校验。
- 上传完成但缺少必要元数据时，不再误标记为“已上传”，改为回到待补传状态。

### 新增
- 新增 AList 对账并发参数：`remoteVerifyConcurrency`。
- 新增 AList 对账限速参数：`remoteVerifyRateLimitPerSecond`（次/秒）。
- 新增每轮最多补传参数：`remoteRequeueLimitPerCycle`。
- 新增远端校验退避字段：`nextRemoteCheckAt`，用于失败后延迟复查。

### 修复
- 修复“仅按局部数据对账导致误判已上传”的问题。
- 修复远端路径缺失时无法确认文件状态的问题。
- 修复“添加 B站账号”按钮文案乱码。

## [2.1.3] - 2026-05-10

### 新增
- 新增“状态对账（仅 AList）”按钮，用于只拉取 AList 侧状态并同步本地标记。
- “全量扫描并对账”加入二次确认与风险提示。

### 修复
- 优化手动触发对账日志与统计输出。
- 增加无新增视频时的周期日志，便于判断任务是否正常运行。

## [2.1.2] - 2026-05-10

### 修复
- 修复全量对账覆盖范围问题。
- 修复部分 Web 参数未实际生效的问题。

## [2.1.1] - 2026-05-10

### 修复
- 修复 AList 状态回查缺失导致“已删文件未回补”的问题。
- 增强远端缺失回补链路，补齐“无新增也输出运行日志”。

## [2.1.0] - 2026-05-10

### 重大更新
- 重构持续备份链路，支持后台长期运行与重启恢复。
- 优化风控场景下的重试与调度，降低同步中断概率。
- 强化收藏夹与备份状态一致性。

## 历史版本

`v2.0.2` 及更早版本的变更请参考 Git 标签与提交记录：
- `v2.0.2`、`v2.0.1`、`v2.0.0`
- `v1.0.9` 至 `v1.0.0`
