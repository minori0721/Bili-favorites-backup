# Dev 测试说明

## 2026-09-23 2.6.2 发布准备

- 本版整合调度、存储、HTTP 与数据解码边界收口，以及下载清单损坏隔离、B站列表按接口解码、旧画质档案分组迁移和测试文件去重。正常 HTTP 契约、SQLite schema 11、JSON 状态 schema 13、迁移包 schema 3、存储键和用户流程保持不变。
- 最终业务快照的服务端、浏览器和测试 TypeScript 检查通过；架构检查扫描 330 个源文件通过；自动测试 898 项中 896 通过、2 项因本机缺少 aria2c 和 Windows 链接能力跳过、0 失败；Playwright 三视口 294 项中 223 通过、71 项按设备条件跳过、0 失败。应用及文档构建、`git diff --check` 通过。
- 2026-09-23 重新执行根项目和文档站完整依赖审计，均为 0 项已知漏洞。Linux 镜像构建、隔离启动、dev 测试证据复用和正式标签发布以本次发布提交的 GitHub Actions 为最终依据。

## 2026-09-22 架构边界与生命周期最终收口

- 应用入口已收敛为组合根：`src/index.ts` 只负责基础设施与服务组装、路由注册和启动/关闭顺序，账号、收藏、归档、播放、迁移、清理、探测、更新及队列接口均由独立服务和 `src/http/` 路由承接。`SyncScheduler` 保留稳定兼容门面，调度准入、同步、探测、传输、恢复、画质、维护及查询投影由对应工作流和窄接口持有。
- 状态和存储边界已明确：仓储负责领域数据读写，持久 JSON 在边界按领域校验；`StateManager` 保留兼容委派、内存状态和 `runAtomic` 回滚支持。维护操作使用带身份租约，旧回调不能解除新维护；数据库替换只有在工作排空、适配器重绑定和恢复准备全部成功后才重新开放调度。
- 启动、停止和失败语义已收紧：关键恢复失败时不开放任务准入，关闭超时不伪装为空闲；迟到回调由运行代次、租约和持久证据拒绝。可降级后台工作产生结构化错误，持久数据损坏、事务失败、重绑定失败和恢复失败继续向上报告，不再变成空数据或成功状态。
- 测试改为通过公开服务、工作流端口、隔离 SQLite 和可注入适配器验证行为；测试 TypeScript 检查和架构检查禁止显式 `any`、调度器私有成员访问、静默恢复、解析器空集合兜底、组合根内联路由、业务依赖整个调度器及运行时循环依赖。历史 failure-boundary 放行清单已删除；资源所有权清单登记停止、迟到回调和重绑定规则。
- 对外兼容边界保持不变：没有修改 HTTP 地址、SQLite schema、存储键、任务状态机或用户操作流程；没有引入新框架、ORM、消息队列或多进程架构。详细职责和新增功能入口以 `docs/development/architecture.md`、`resource-ownership.json` 和 `scheduler-ownership.json` 为准。
- 推送前最终本地验收：服务端、浏览器和测试 TypeScript 检查通过；架构检查扫描 325 个源文件通过；自动测试 859 项中 857 通过、2 项因本机缺少 aria2c 和 Windows 链接能力跳过、0 失败；Playwright 三视口 291 项中 220 通过、71 项按设备条件跳过、0 失败。应用及文档构建、根项目依赖审计和 `git diff --check` 通过；根项目审计为 0 项漏洞。
- 文档工具链全依赖审计仍有 12 项既有开发依赖告警（9 moderate、3 high），本轮没有为架构重构静默升级整套工具链。Linux 镜像构建、隔离启动和当前提交的 GitHub Actions 以推送后的 dev 工作流为准，本地结果不替代该证据；本轮未部署服务器、未修改 main、未创建标签或 Release。

## 2026-09-09 2.6.0 版本准备与 CI 验收复用

- package.json 与锁文件统一升级至 2.6.0；将已完成变更归入对应 CHANGELOG 和文档站章节，保留完整细节、简版发布摘要与升级回滚说明。未发布章节清空；尚未创建正式 tag。
- main push 仅在同一仓库、同一工作流、同一 SHA 的 dev push 全部成功后复用 npm test；缺少记录或查询异常执行全套测试。dev、tag 和手动运行始终测试，镜像构建及隔离冒烟始终执行。工作流自身改动不再被 paths-ignore 排除。
- 相关测试 8/8 通过，应用构建、227 个源文件架构检查及文档构建通过；v2.6.0 发布摘要生成通过。只读查询真实 dev run 34266492522 确认 API 字段与判定一致。新工作流尚未推送运行，不将本地验证视为线上 CI 验收；未重复执行业务全量测试或 UI。

## 2026-09-09 dev → main 合并验收

- 文档站未发布章节同步重构、恢复/会话修复、响应式布局及归档 UID 修复，补充升级、回滚及源码构建要求；文档构建通过。
- 最终业务代码全量 UI 运行 288 项：216 通过、71 视口条件跳过、1 项测试时序失败。Trace 显示关闭按钮自动化点击耗时约 323ms，超过 300ms 搜索延迟，请求实际发生在关闭前。将“输入后立即关闭”改为同一浏览器事件轮次，保留关闭后不得发出搜索的断言；该项连续重复 10 次全部通过。业务代码未因本次测试调整而改变。完整结果分别见 output/main-readiness-ui-complete.log 与 output/main-readiness-search-repeat.log，不将专项复验写作另一次全量运行。
- 一次更早的全量运行在消息切换时中断，未计为通过。后续完整运行使用独立后台进程，退出状态单独记录。
- 修复提交 0f9a9f9 的 Linux CI 732 项全部通过，构建、依赖审计与发布镜像隔离冒烟通过；服务器归档 116 页/5779 个视频及 7 个收藏夹响应校验通过。本次先推 dev、确认流水线后快进 main，再等待 main 流水线；不创建正式标签，main 构建成功后更新 latest。

## 2026-09-09 归档导航 UID 契约修复

- 修复前端导航校验误将后端数字 UID 要求为字符串的问题；保留后端 API，接受非负安全整数（历史移除账号可能为 0），拒绝非法类型及数值。
- UI 假服务使用真实数字 UID。新增真实 SQLite 归档查询经 JSON 序列化后交给前端校验的回归，覆盖普通/移除账号、停用收藏夹与全局/账号/收藏夹列表。
- Windows 本地构建、类型与架构检查通过；归档及契约测试 15/15 通过，桌面/手机/横屏归档与可访问性 UI 回归 19 通过、23 视口条件跳过、0 失败。日志：output/archive-uid-build.log、output/archive-uid-tests.log、output/archive-uid-ui.log。
- 故障表现：`dc518ff` 的归档导航在解析数字 UID 时抛出“归档账号 UID 格式错误”，导航初始化中断，后续视频列表未加载。只读检查确认当时数据库与查询正常：5779 个视频、4904 个可播放视频、10374 条已验证远端文件记录；这些统计不代替远端实体文件检查。
- 遗漏原因：原 UI 假服务使用字符串 UID，单元测试又省略 UID，未覆盖真实接口与前端边界组合。修复保持接口地址、后端响应和数据库不变，无需重建索引或恢复备份；部署修复镜像后刷新页面，验收目录、首批列表及分页。
- 本补丁按 dev 流程提交；服务器是否已更新以实际运行提交为准，不将 CI 开始或源码推送成功视为线上修复完成。

## 2026-09-09 首页响应式布局修复

- 看板按容器宽度以 532px / 1076px 为断点切换 1 / 2 / 4 列，保留阶段顺序，移除移动端 82vw 横向滑动布局。全空队列收紧至 112px，无多余滚动条；有任务时保留等高列和列表内纵向滚动。
- 页头移除移动端强制整行规则，617px 保持单行，窄屏自然换行，长版本号省略。账号区统一使用 16px 间距，空状态提示不占位，错误提示仍正常显示。
- Windows 本地应用构建、类型与架构检查通过；针对性 UI 回归 54 项：45 通过、9 视口条件跳过、0 失败。覆盖 320–1280px、用户反馈尺寸、多个账号、长版本号、错误提示、空队列与大量任务，以及原有看板、资源加载、可访问性回归。日志：output/layout-build.log、output/layout-ui.log。
- 已在本地隔离测试服核对 617×620 和 1045×792 实际页面。未执行 WSL、后端全量测试、提交、推送或部署。

## 2026-09-09 Review 修复：恢复下载与会话失效

- 恢复重下载与旧版失败下载使用明确的 `prepareRecoveryDownload`，不再根据残留完成清单转成上传。准备结果区分下载、上传与权限探测；提交失败保留原任务、会话和失败证据，已有不可自动执行的下载任务不会被误认为成功接管。普通备份继续保留本地成品复用行为，不删除旧媒体目录。
- 受保护 HTTP 请求统一经过会话通道，覆盖 JSON API、更新检查及导出。并发 401 仅触发一次失效处理，在解析正文前识别；取消在途请求并卸载前端功能，停止轮询、日志重连和播放器，显示明确的重新登录入口。不重放旧操作，提示用户已提交的后台任务可能仍在运行。403/409/503 与普通断网保持原错误处理；日志断线通过同一通道确认认证状态。
- 新增真实 SQLite 与真实任务准备模块组合测试，覆盖完成清单仍在但文件缺失/改变、事务回滚及重复恢复；补充并发 401、非 JSON 401、迟到成功、取消请求、静默提示、日志停止与三视口登录失效行为。
- 最终全量后端 730 项：728 通过、2 环境跳过、0 失败；UI 三视口 279 项：210 通过、69 视口条件跳过、0 失败。应用构建、服务端/浏览器类型、227 个源文件架构检查、文档站构建通过。日志：output/review-repairs-tests.log、output/review-repairs-ui.log、output/review-repairs-build.log、output/review-repairs-docs-build.log。
- 修复后隔离 Linux 镜像重新构建并通过资源鉴权、HTML 缓存、JS/CSS 摘要、媒体生成/探测及正常停止重启验收；network=none、mounts=0、exit=0，临时容器已清理。镜像 ID：sha256:556b39d085c1e62bc6a0be0563998e9d2976515d606b2b63e1e8e13939e04a78。沿用前次摘要一致的固定 FFmpeg 本地资产阶段；日志：output/review-repairs-linux-build.log、output/review-repairs-linux-smoke.log。
- 修改保留在本地 dev，未提交、推送、发布或部署；依赖未改变，本轮不将先前依赖审计结果冒充新一次审计。

## 2026-09-08 可维护性重构（本地实现与验收完成）

### 最新检查点

- 恢复动作、启动恢复、缓存扫描、账号退役与来源删除协调已迁入对应模块；恢复、账号删除及归档删除 HTTP 路由使用窄接口装配。下载/上传任务工厂核验运行代次，旧回调不能跨重绑定继续写入。
- 冲突选择和放弃共用任务锁，候选决议、归档证明、会话收尾及任务完成原子提交。恢复重下载先在事务外准备本地证据，再把来源重置、替代任务入队及旧任务收尾一起提交；旧版失败下载也保留失败证据直到替代任务提交成功。新增真实隔离 SQLite 拒绝提交与回滚断言。
- 状态所有权清单与开发指南已同步，移除无调用者的旧私有入口和旧测试调用方式。历史检查点中的“仍需迁移”仅描述当时快照，当前结构以 docs/development/architecture.md 为准。
- 最终稳定代码完整后端回归：722 项，720 通过、2 项环境跳过、0 失败；日志 output/refactor-complete-backend-tests.log。三视口 UI：204 通过、69 项视口条件跳过、0 失败；日志 output/refactor-final-ui-tests.log。UI 后仅修改后端路由、恢复事务及开发文档，浏览器代码保持该已验收快照。
- 应用构建、服务端/浏览器类型检查、226 个源文件架构检查、文档站构建和差异检查通过；根生产依赖审计为 0 项漏洞。日志 output/refactor-complete-build.log、output/refactor-complete-docs-build.log、output/refactor-final-production-audit.json。文档工具链遗留开发依赖告警独立保留，未升级整套依赖。
- 最终 Linux 镜像已重新构建并通过隔离验收：`bfb-refactor-check:local-complete-20260908`，镜像 ID `sha256:e62beaa0819dbc5cba922f831848d186665b6d9822ded6bf72e0eb4d5f0b3f40`。登录、未认证接口/业务资源保护、主页面 no-store、哈希 JS/CSS 摘要及私有长期缓存、已认证配置读取、媒体样片生成/探测、Tini 和 aria2 均通过。同一隔离数据正常停止、重启后重复通过，两次退出码均为 0；network=none、mounts=0，临时验收容器和此前遗留构建容器已确认不存在。
- 外网下载固定 FFmpeg 曾阻塞、审批额度曾暂时耗尽；恢复后使用仅位于 output 的临时 Dockerfile 复用同版本本地资产阶段，应用构建及运行阶段仍来自当前 Dockerfile。压缩包 SHA256 与锁定值一致，容器内 ffmpeg/ffprobe 二进制摘要也与已校验资产一致；没有修改正式下载地址或跳过摘要验证。首轮样片脚本误用 LGPL 构建不含的 libx264，改用原生 MPEG4 编码器后通过，不涉及应用代码变更。日志：output/refactor-complete-linux-build.log、output/refactor-complete-linux-smoke.log。
- 本地实施与验收完成，没有提交、推送、发布或操作真实服务器。代码及既有首屏修复保留在本地 dev；正式发布仍按 dev → main → tag 流程，发布流水线须再次验证最终发布 digest。没有把本地镜像 ID 当作已发布镜像摘要。

### 历史检查点：队列与恢复边界继续迁移

- 下载/上传的开始、重试、失败和完成业务迁入 task-progress、download-failure、upload-failure、download-completion、upload-completion；原构造函数从本轮接续时的 1051 行降至 158 行，保留依赖组装与接线。同步事务整体迁移，不拆散父子任务转换与归档证明提交。
- 核验任务生成、核验结果与异常处理迁入 verification-jobs 和 verification-handlers；移交字段由 verification-payload 收窄。恢复列表删除重复旧实现，恢复评估和本地证据检查迁入 recovery-assessment、recovery-local-files。
- recovery-automation 拥有轮询及运行中批次，recovery-work 拥有评估请求与人工操作锁。关闭、维护同时等待请求和锁排空；停止或代次变化后不产生下一项自动评估。
- 新增边界解析、重入去重、失败释放和手工锁排空测试；架构检查禁止包含 any 的类型断言。队列处理器检查点全量 707 项，705 通过、2 环境跳过；该记录早于后续核验/评估迁移，后续结果以对应完整测试日志为准。
- 当前仍有恢复动作执行、启动恢复、账号退役/来源删除协调及相关路由的迁移工作；不将本次接线完成等同于全套重构完成。没有推送、发布或操作服务器。
- 上述核验、恢复评估与并发所有权迁移后的完整回归为 712 项、710 通过、2 环境跳过、0 失败；统一构建（含服务端/浏览器类型和 194 个源文件架构检查）及差异检查通过。日志：output/refactor-recovery-handlers-tests.log。本段代码未变更浏览器，未重跑 UI 或重建 Linux 镜像；旧 UI/镜像记录属于先前快照。

- 构建、状态边界与剩余工作以 `docs/development/architecture.md` 和 `scheduler-ownership.json` 为准；文档明确区分当前所有者与迁移目标，不把文件拆出等同于全套完成。SQLite、JSON 状态和迁移包 schema 未改变。
- 设置、清理、迁移导入导出、重命名、画质维护、在线内容等前端功能迁入独立严格 TypeScript 控制器；进一步迁移收藏夹详情、下架分页和视频卡片。关闭取消请求，代次拒绝迟到回写；复核请求按展示区域释放。
- 实际手机双击测试发现第二次点击会落到新出现的确认按钮。公共确认组件拒绝后续点击，回归明确断言单独确认前没有提交请求；公共弹窗管理器统一拥有遮罩事件和焦点循环。
- 画质候选判断、预览和提交复核迁入窄接口服务，HTTP 路由保留统一维护包装器；完整校验一批请求后才开始提交。原调度器中的扫描、恢复、画质执行、清理及队列业务回调仍有剩余迁移工作。
- 本轮维护功能检查点：后端 658 项，656 通过、2 环境跳过；维护与无障碍 UI 三视口 29 通过、7 条件跳过。随后新增的画质路由、详情与下架清单由专项和新一轮全量 UI 验证，不能将前一检查点冒充最终全量结果。
- Linux 构建阶段已完成干净安装与构建；完整镜像下载固定 FFmpeg 资产时遇到 GitHub 连接超时。运行镜像与升级验收仍未完成，不将 Windows 成功或缺少 FFmpeg 的构建阶段测试等同于镜像验收。所有故障测试使用本地隔离数据，未操作真实服务器。
- 当前未提交、推送、发布或部署。既有文档工具链依赖告警独立保留，没有为重构静默升级整套依赖。

### 2026-09-08 继续收口

- 后端继续拆出 `local-capacity.ts`、`access-probes.ts`、`remote-scan.ts`、`quality-rules.ts`、`recovery-projection.ts`、`recovery-commit.ts` 和 `local-cleanup-plan.ts`。恢复确认现在把旧归档证明、传输会话和任务完成放在同一个同步事务边界，失败会回滚；远端对账等待所有已启动 worker 收敛后才结束。专项新增测试通过：容量 3、探测 41、远端对账 2、恢复提交 4、恢复投影 2、画质规则 2；本地清理与释放路由 24 项通过。
- 当前阶段最终全量自动测试为 698 项，696 通过、2 项环境跳过、0 失败；三视口 UI 为 273 项，204 通过、69 项按视口条件跳过、0 失败。根生产依赖审计为 0 项漏洞。新增恢复投影、质量规则和清理授权的代码均已包含在该快照。
- 当前 dev 代码用隔离 Linux 镜像重新构建成功（本地镜像 `bfb-refactor-check:local-current-20260908`，临时容器无网络、无宿主挂载），验证登录页、未授权接口、监听日志及 `ffmpeg`/`ffprobe`/`tini`；没有连接真实 B站、WebDAV 或服务器。
- 队列生命周期订阅接线迁入 `src/scheduler/task-event-bindings.ts`，调度器只提供开始、完成、失败和排空处理器；定向调度/恢复/上传验证回归 54 项通过。
- 画质任务重建与生命周期回调接线迁入 `src/scheduler/quality-task-factory.ts`，恢复目标及旧版失败键解析迁入 `src/scheduler/recovery-identifiers.ts`；调度器保留显式依赖组装，避免任务构造闭包隐式持有整个调度器。新增纯解析测试 2 项；随后全量后端测试为 700 项，698 通过、2 项环境跳过、0 失败。

- 前端旧入口已删除，浏览器全部使用严格 TypeScript；任务中心、归档库、播放器迁移完成。样式按功能归属导入，验证完整压缩 CSS 与原始样式一致。前端旧入口删除后的整套 UI 为 198 通过、69 条件跳过；随后页面退出与生命周期专项 9 项、播放器回退及旧实例事件专项三端 3 项通过。
- 收藏扫描及本地清理迁为独立服务，时钟、准入、代次与外部适配器通过明确接口注入；同步扫描的迟到页和封面回调失效，清理在维护或代次改变后保留文件与授权。本地释放路由统一进入维护边界。当前扫描、清理、路由专项共 23 项通过。
- 本轮中间全量自动测试为 676 项，674 通过、2 环境跳过；该结果早于扫描和本地释放路由的最后调整，不能代替最终快照全量验收。后端探测、远端对账、恢复、画质执行和队列业务回调仍未全部迁出。

- 归档库与播放器响应分别经过 `src/shared/api/archive-library.ts`、`src/shared/api/playback-queue.ts`。来源身份要求账号和目录编号齐全；假服务补齐真实契约，不为轻量夹具放宽校验。详情目前复用项目解析，不将其称为完整详情契约验收。合法的空队列焦点 `-1` 和手动归档目录 `-1` 已补真实 SQLite 响应回归。
- Linux 运行镜像构建改为从构建阶段复制依赖后执行 `npm prune --omit=dev`，解决精简运行镜像没有 Python 时重新编译 `better-sqlite3` 的失败。使用隔离、无网络、无宿主挂载容器验证到达 `Server listening`；`ffmpeg`、`ffprobe` 和 `tini` 均可执行。容器已清理，未操作真实数据。
- 前一检查点验收（不代表后续改动的最终结果）：`npm test` 共 669 项，667 通过、2 条环境跳过、0 失败；Playwright 三端 264 项，195 通过、69 条条件跳过、0 失败；当时的架构检查、浏览器类型检查、应用构建、依赖审计和 `git diff --check` 通过。本地镜像 digest 为 `sha256:f31d2b49b6a3d701768cb332b51bd3509d79fa046a129e1898292edf4a37e0ac`，只用于该检查点，未推送；HTTP 登录与资源冒烟尚未成功执行。
- 看板轮询、视图与恢复操作迁入 `board-controller.ts`、`board-view.ts`、`queue-recovery.ts`。轮询拥有请求代次、时钟及失败重试；视图拥有卡片缓存、诊断展开状态和动画；恢复操作按任务去重，关闭后取消并拒绝迟到通知。旧入口移除对应实现。看板展示类型显式保留可选字段，不再把稀疏响应强转成完整业务任务。
- 本次看板检查点：浏览器模块与真实 SQLite 播放专项共 61 项通过；最终看板 UI 桌面、手机、横屏共 24 项通过，包含连续恢复点击去重和 503 后重试。此前同轮看板与页面状态组合 UI 为 41 通过、25 条件跳过。当前构建、严格浏览器类型检查、架构检查和差异空白检查通过；全套重构与最终全量验收仍未完成。

## 2026-09-07 看板首屏修复（本地dev）

- 初始HTML隐藏日志并输出四列加载结构；客户端复用列节点，看板请求提前到初始化请求之前发起。失败保留结构与未知计数，提供手动重试并保留原自动轮询。
- Playwright CLI使用隔离假服务验证禁用JavaScript首屏、队列延迟、列节点复用、启动单次请求、日志切换、503失败后重试及390px布局；未触碰真实账号或远端服务。截图和脚本位于output/playwright，未提交。

## 2026-09-07 v2.5.6发布准备

- 以dev修复提交876f794为基础，整理根版本/锁文件、README、CHANGELOG、文档版本记录及导入恢复和回滚说明；SQLite 11、JSON状态13、迁移包3和BBDown bfb-2.0.5不变。
- Release提取同版本的“发布摘要”和“升级与回滚”，保留历史章节兼容；摘要存在时缺失/重复/空章节拒绝生成。Docker推送前增加正文与版本预检；推送后在Actions runner上以精确digest启动无网络、临时data/temp容器，检查revision、登录页及未授权接口，成功后才允许Release。
- 当前应用全依赖、文档生产依赖审计为0项；文档全依赖5项（2 high、3 moderate），涉及nanoid、PostCSS和Vite/VitePress/esbuild链，保留为明确限制。静态站构建已通过，不在发布收口时扩大到文档构建工具链升级。
- 最终本地验证：npm test 607项，605通过、2环境跳过、0失败；三视口UI135通过、63条件跳过、0失败。应用/文档构建、YAML解析、版本锁文件一致性和git diff --check通过。日志在output/release-256-*，不提交。Linux镜像与新冒烟步骤由发布提交的Actions另行验收。
- 发布按dev、main、正式标签顺序等待各轮工作流完成，避免本次发布任务互相覆盖可变镜像标签；尚未改变Docker并发控制策略。服务器不包含在本次发布操作中。

## 2026-09-07 本地代码审查修复：导入恢复与封面清理

- 修复首次数据库重命名失败时误删原库：替换前使用SQLite backup API准备新旧独立快照（包含已提交WAL数据），校验并同步文件后写入替换记录。启动在正常建库前恢复；缺少或损坏恢复证据时拒绝建立空库。
- 新增`data/import-transaction.json`和数据库旁的`.replacement.json`作为本轮导入/替换恢复记录，文件路径由操作ID和固定目标生成。配置、账号、封面及temp的切换与数据库采用同一提交决定；未提交时回到旧版，已提交时保留新版。提交后备份清理失败不反向回滚；恢复失败保留快照及记录并阻止业务继续写入。
- 导入先接收并验证隔离压缩包，再取得维护权并排空已接纳的异步API/账号刷新；重新检查队列、媒体探测、归档删除和路径迁移状态，等待封面与重命名预览结束。维护期间API返回409，迟到扫码回调不写账号；后台令牌刷新延后。调度清理锁覆盖队列启动和本地清理扫描，归档删除重载期间暂停调度。
- `return await`让路径迁移保护覆盖导入与回滚全程；新增持锁内部rebind入口，普通rebind仍拒绝忙碌状态。真实应用冒烟首轮因此发现重载把自身维护锁误判为忙碌，已修复并保留真实导入验收。
- 封面临时目录清理采用有限重试，清理失败记录脱敏告警且始终释放并发名额；成功封面不因临时清理失败变成失败。清空/淘汰仅在删除成功或ENOENT后扣减占用，失败保留条目与占用并允许重试；stat未知错误不视为文件已删除。
- 新增故障注入、子进程中断恢复、配置与数据库成对恢复、提交后清理失败、恢复失败后再次启动恢复、维护期间真实接口409、封面并发及占用回归。所有数据均在本地隔离目录，无真实B站/WebDAV访问、服务器操作或真实归档删除；业务schema和版本不变。上一轮依赖修复保留，未提交、push或部署。
- 日志与测试输出在`output/import-*`。中断测试覆盖进程退出，不将其等同于真实断电、硬盘故障或Linux镜像验收。回退代码前必须完成或核实恢复记录，不能用旧版本跳过未完成的导入恢复；正常无恢复记录时可回退本轮源码，数据库业务schema未变。
- 最终全量`npm test`：606项，604通过、0失败、2环境跳过（aria2c缺失、Windows软链接/junction权限）；新增中断恢复及真实接口并发断言均通过。应用构建、文档构建、git diff --check通过。三视口UI135通过、63条件跳过、0失败；UI运行后仅补充后端恢复记录/清理边界及测试，最终后端由上述全量覆盖，未改前端代码。浏览器测试端口43249已退出。

## 2026-09-07 依赖安全修复（本地dev）

- 复核原任务最近验收流程：依赖此前仅审计、未升级；本轮在`148b911`上单独修复，保留既有预览与output产物，不提交、push或部署。
- package.json使用明确版本的overrides：bili-api下Axios 1.18.0、fast-xml-parser 5.7.3；body-parser 1.20.6、qs 6.16.0、ip-address 10.7.0；brace-expansion的2.x和5.x分别固定2.1.4和5.0.9。直接依赖版本保持不变，未运行audit fix --force；必要子依赖随锁文件更新。
- qs修复超出Express/body-parser的`~6.15.1`范围；XML修复超出bili-api的4.x范围，因此两项需要显式兼容验收。XML仅覆盖bili-api依赖，WebDAV仍使用原有5.7.3并去重。后续上游解除版本限制时，应重新评估并移除相应override，不能永久固定后不再审计。
- 新增真实依赖兼容测试：调用bili-api公开utils将protobuf弹幕转XML，检查中文、四字节字符与标签转义；本机HTTP夹具检查Axios重试与Cookie/查询参数保留；Express检查嵌套表单、数组查询、原型键过滤和超限JSON的413响应。不请求真实B站或WebDAV，不使用账号凭据。
- 本轮根项目全依赖、生产依赖及文档站生产依赖审计均为0项；这是当次npm公告库结果，不代表所有未来风险消除。验证日志与独立安装目录在`output/dependency-*`，不提交这些产物。应用版本2.5.5及各数据schema不变。
- 最终验收：完整npm test为593项，591通过、0失败、2环境跳过（缺少aria2c、Windows软链接/junction权限）；Playwright三视口135通过、63条件跳过、0失败。应用构建、文档构建及git diff --check通过。独立目录npm ci --ignore-scripts成功且锁文件未变；该项只验证从零依赖安装，不代表重新构建原生模块或Linux镜像。运行测试使用现有本机原生模块，未执行线上验收。
- 回退只需恢复本轮package.json/package-lock.json配对改动并按原锁文件重新安装；不涉及数据库或远端文件回退。安装提示prebuild-install与node-domexception弃用，审计未报告它们存在漏洞；后续由所属上游升级处理，未扩大本轮替换范围。

## 2026-09-07 失效展示与媒体探测边界修复（本地dev）

- 增加仅展示用的archivedSourceUnavailable字段，对齐SQLite筛选/计数及状态层统计；保留当前关系的本人可见和手动来源例外，不修改unavailable下载判断。线上只读调查曾发现164个已验证归档被收藏夹标记失效但详情状态待确认，该数字不是确认删除的视频数。
- MediaProbeService区分unknown与unavailable，旧布尔返回缺少三态证据时采用未知提示。BBDown互动及播放协议信号要求独立行；探测和正常下载流使用UTF-8解码，输出大小限制与严格规格校验保留。
- 队列封面统一为本地优先、远端回退、最终占位，候选不变时不重设src；更新弹窗按nextRefreshAt显示冷却并在关闭时清除计时器。没有修改后端更新频率。
- 新增字节级Fake BBDown、标题伪信号、失效筛选分页/重启/恢复与跨来源隔离、封面回退和更新冷却测试。只使用本地隔离数据，README草稿保留，不提交、不push、不部署。
- 首轮完整测试暴露3个信号格式兼容回归，已对照BBDown fork保留标准时间戳前缀和APP_NO_VIDEO_INFO说明行，并增加回归用例；不是放宽为任意子串匹配。
- 最终验收：npm test共590项，588通过、0失败、2环境跳过（缺少aria2c、Windows测试软链接权限）；CLI Playwright三视口135通过、63条件跳过、0失败。应用构建、文档构建和git diff --check通过。测试日志在本地output/review-fixes-final-*.log，不作为发布产物提交。
- 本次实时生产依赖审计：根项目12项（6 moderate、6 high），文档站0项；根项目及文档站依赖和锁文件均未修改，不沿用旧记录中的8项数字，也未自动升级依赖。

## 2026-09-07 BFB 2.5.5发布准备

- 按既有流程从dev快进main并创建正式注解标签；不手动部署服务器。未发布章节恢复为空，历史版本记录保持不变。
- 同步根包与锁文件版本、README及当前兼容性说明，内置BBDown为bfb-2.0.5；SQLite 11、JSON 13和迁移包3不变。
- 发布前dev提交9ac1a13的Linux Docker工作流585项测试全部通过，文档工作流成功；本地此前完整UI123通过、63条件跳过。以上为发布版本整理前的验证，最终标签工作流需另外核验。
- 已知生产依赖风险仍为根项目8项（5 moderate、3 high）、文档0项，不在版本发布中进行自动依赖升级。
- 发布准备两轮本地全量均为582通过、1失败、2环境跳过，失败为manual local release测试仅等待unlink就断言异步数据库收尾完成；独立16/16通过。已将等待条件收紧为文件消失且持久清理计划清空，保留原断言，不修改业务清理逻辑。

## 2026-09-06 BFB Release补齐与混合发布兼容（本地dev）

- 根因是仓库此前只有FFmpeg Release，BFB版本仅有Git标签。已核验远端v2.5.4注解标签指向`941a968`，对应Docker标签工作流33980135356成功；从原标签CHANGELOG章节生成并补建BFB v2.5.4 Release，设为Latest。没有创建或移动标签、重建镜像、发布新版本或部署服务器。
- FFmpeg Release显式设为非Latest，修改前后公开元数据对比确认正文、标签、附件ID、名称、大小、摘要与下载URL不变；备份位于本地`output/ffmpeg-release-before.json`。其他历史BFB版本未批量补建，可通过CHANGELOG查询。
- 新检查器正常只请求Latest一次；遇到工具、预发布或无Latest时，最多查询3页、每页30项，只接受`vX.Y.Z`正式应用版本。分页链接不作为任意请求目标；总时限8秒、单响应1MB、总2MB，未完整结果不产生最新版本判断。保留缓存、限流退避和安全错误分类；main镜像也按稳定渠道比较。
- 新增锁定的markdown-it解析器，禁用原始HTML、远端图片、自动链接和表格；链接仅HTTP/HTTPS且不携带URL凭据。正文最长24000字符，空正文、截断和旧缓存明确显示，CHANGELOG链接绑定对应标签。无额外浏览器CDN请求。
- 新发布脚本从标签对应CHANGELOG提取单一章节，校验package版本，添加固定镜像标签与文档链接。Release job只在正式标签的Docker job成功后运行，写权限仅限该job；重跑保留既有Release，旧版本不覆盖较新Latest。工作流YAML已解析并核验依赖/权限边界；真正的GitHub自动发布job需代码推送并在以后正式标签时验证。
- 真实公开查询：本地新检查器约744ms，Aliyun现有镜像检查器约795ms，均读到v2.5.4、2123字符正文且无错误；服务器只读运行独立检查进程，没有改配置、任务、文件或服务。
- 新增专项10项通过；全量首轮581通过、1失败、2环境跳过，失败为既有local-cleanup启动清理测试的文件删除与数据库收尾时序。该组独立16/16通过，完整复跑584项：582通过、0失败、2环境跳过。没有修改清理业务或该测试。
- UI首轮119通过、1失败、63条件跳过：旧统一视觉断言只接受sticky标题，不适用于更新窗口的独立滚动布局；现已保留其他弹窗断言并检查更新窗口的static标题、内部滚动和隐藏外部溢出，最终全量结果在下方补记。
- 最终完整UI123通过、63条件跳过、0失败，覆盖空正文、截断、缓存错误和Markdown展示。最后仅对格式错误冷却增加条件保护，不再误用仍有额度时的GitHub限流重置时间；该最后改动由11/11专项测试及应用构建覆盖，全量单元结果对应此条件保护之前的快照。发布脚本对真实既有v2.5.4的幂等检查返回Existing release preserved，未覆盖正文。
- 应用构建、文档构建及差异检查通过。生产依赖审计根项目8项（5 moderate、3 high），新增Markdown依赖未列入风险；文档生产审计0。未执行自动依赖修复。
- 隔离假数据预览43236，三视口截图无横向溢出；预览不是线上Release数据。本地代码、脚本、文档和锁文件未提交、未push，main与线上镜像保持不变。

## 2026-09-06 版本与更新（本地dev）

- 管理员会话及同源保护下新增 `GET /api/updates`，固定读取本仓库GitHub最新正式Release，不接收上游URL或凭据；8秒超时、512KB响应上限、禁止重定向，发布说明截断至24000字符并作为纯文本展示。接口只返回安全摘要。
- 首次打开才请求，进程内缓存6小时，手动刷新最短1分钟、失败冷却及限流退避最长1小时；并发请求合并，失败保留上次成功结果。重启丢弃缓存，无新文件或schema，不接入同步队列。版本按数字比较，只有构建ref等于版本标签时判断正式升级；dev/local仅参考，不探测镜像或执行升级。
- 弹窗复用焦点栈，关闭取消前端请求并隔离迟到响应；中部独立滚动，保持标题与底部按钮无遮挡。隔离假数据预览43234，发布内容明确为测试数据，不代表GitHub实时状态。
- 单元与集成测试578项：576通过、2既有环境跳过、0失败。新增服务专项4项通过；最终更新窗口三视口9项通过，覆盖纯文本、缓存策略对应交互、错误与迟到响应及布局边界。应用构建、文档构建通过。完整UI结果另记，未将截图初轮发现的透明标题穿透问题作为验收通过。
- 完整UI117通过、63条件跳过、0失败（开始于中部滚动修复前）；最终布局和迟到请求保护另由更新窗口三视口9/9覆盖，未声称完整UI运行于最后快照。`git diff --check`通过，仅既有换行警告。
- 本轮不增加依赖，未重复已有依赖审计；未提交、推送或部署。

## 2026-09-06 主页与控件精修收口（本地预览）

- 主页顺序调整为账号、任务中心、设置；设置改为6个原生折叠组，默认展开同步节奏。保存仍一次读取全部字段，隐藏的无效字段先展开并定位；从待处理进入存储设置也会展开目标组，不改变配置保存和任务逻辑。
- 看板区分阶段与原因，长技术错误可展开查看，保留完整错误信息及展开状态；相同阶段与时间标签不再重复。统一次要按钮、关闭和返回控件的尺寸与焦点，保留玻璃配色和原圆角。
- 手机规格选项分开展示尺寸帧率、覆盖范围、大小来源与字节数；使用独立Fake服务检查桌面、手机与横屏，不连接真实B站或WebDAV。
- 完整单元与集成测试574项：572通过、2项既有环境跳过、0失败。完整CLI Playwright三视口111通过、63条件跳过；随后仅调整重复时间标签，又运行看板专项三视口15/15通过。
- 应用构建与文档构建通过。没有依赖变更，本次未重复依赖审计，上节已有审计结果不作为本次新扫描。
- 最终预览地址为本机43232端口，替代上一轮43230；截图和测试输出位于 `output/polish-all-*`，不提交产物。本轮未提交、推送或部署。

## 2026-09-06 待处理与规格弹窗精修（本地预览）

- `recovery-issues-detail`明确使用block和全宽，避免继承主页main的grid间距、最大宽度及自动居中。合并重复的标题元信息，文件信息改为无卡片的双列区域，保护说明放到操作之前；技术详情仍折叠，事件与危险确认不变。
- 统一重新探测按钮的玻璃样式、键盘焦点及禁用状态，提高规格元信息与大小字号；隐藏空错误行及带hidden的旧编码排序编辑器。后者原先被display:grid覆盖，是空白横条的实际原因。
- 玻璃感、青绿色、原圆角、主页布局和全部任务逻辑保持不变。归档标签中的长不可见原因缩短，详情仍保留完整解释。
- 完整UI首轮104通过、63条件跳过，1项因旧说明文案断言失败；更新为新文案并保留严格匹配与旧档保护语义断言后，整组待处理三视口31通过、14条件跳过。另有新增布局专项三视口3/3通过。未把首轮报告冒充全绿。
- 应用构建、git diff检查通过；本轮纯展示改动未重复运行后台全量单元测试，上一轮源站分类测试结果独立保留。
- 当前源码隔离截图在 `output/polish-after-*.png`；假数据预览端口43230，仅本机访问，不连接真实B站或WebDAV。未提交、推送或部署。

## 2026-09-06 源站原因展示收口（本地dev，未提交）

- 62004和62012分别保留 `under_review`、`uploader_only` 原因，仍属于unknown，不改变退避、账号顺序、休眠与恢复条件。有效分P优先；未知响应矛盾或包含临时错误时保持通用未知，不猜测具体原因。
- 62002展示稿件不可见且具体原因未公开，-404展示未找到视频，不宣称作者删除。归档库和收藏详情继续保留播放优先级、旧档保护提示与立即复核入口，不增加待处理角标。
- 本次没有重新探测、重置或修改线上872条任务，没有新增B站接口或网络请求，版本2.5.4及全部schema不变。CHANGELOG及恢复文档同步更新。
- 完整单元与集成测试574项：572通过、0失败、2项既有环境跳过（aria2c、Windows链接权限）。新增明确原因聚合测试，审核中与仅本人可见完整退避至休眠测试。
- CLI Playwright三视口：105通过、63条件跳过、0失败；新增四类原因、旧档可播放、零待处理角标和无横向溢出断言。使用隔离Fake服务，不连接真实B站或WebDAV。
- 应用构建、文档构建及 `git diff --check` 通过。根项目生产审计12项已有风险（6 moderate、6 high），文档生产审计0项；未改动依赖。
- 验收输出位于 `output/availability-diagnostics-*`，未提交测试产物；本轮未提交、推送或部署。

## 2026-09-06 BBDown 2.0.5 接线（仅本地dev）

- BBDown fork已发布 `bfb-2.0.5`；BFB Docker和下载清单固定提交 `fa7209d63bd73a4ab07913ce1478a0e13056ad09`，Linux x64 ZIP SHA256为 `53c6d06a5679a357d45d974ce8909b81679fe7f11efcb74ecc19107acf0eb08c`，已与下载的正式Release资产核对。
- `2.0.4/2.0.3`在账号、接口和运行配置一致时保留续传轨道，旧版成品仍走CID和ffprobe校验；换账号或改配置仍隔离碎片，更早APP版本的隔离规则不变。新增WEB/APP升级与换账号反例回归。
- 正式Linux Release在WSL2独立目录匿名读取 `BV1pT4y157sf`，返回4个唯一CID，BFB实际解析器通过完成标记与清单摘要校验。只读剧情元数据，未请求媒体流或下载视频，未使用服务器账号。首次测试因WSL无默认FFmpeg而在请求前退出，指定已有Windows FFmpeg路径后通过，无系统安装。
- 正式Release传入错误摘要后输出 `BFB_SIGNAL:INTERACTIVE_CHANGED`，未进入选流或下载；本地Fake回归另行断言失败清单状态及旧文件保留。真实片段的媒体流可用性及完整视频下载尚未验证。
- 最终完整 `npm test`：571项，569通过、0失败、2项既有环境跳过；应用构建、文档构建及差异检查通过。中断前测试没有最终汇总，未计入通过结果，确认无残留测试进程后重新完整执行。
- 最终CLI Playwright三视口：102通过、63条件跳过、0失败，使用隔离Fake服务；未使用服务器数据或浏览器插件。
- 生产依赖审计：根项目12项已有风险（6 moderate、6 high），文档0项；未升级依赖。
- BFB版本仍为2.5.4，各schema不变；本轮未提交、推送或部署BFB，未重试线上任务。稳定版文档说明保留，开发接线单独标注，避免混同当前main行为。

## 2026-09-06 互动视频解析与完整清单保护（未发布）

- 定位对象为 `BV1pT4y157sf`。此前只读诊断已确认普通详情仍有有效CID，而旧BBDown的 `/x/player.so` 返回404；不能把剧情接口失效直接解释成视频已删除。本轮未连接服务器、未重试线上任务、未下载真实视频。
- BBDown fork改为player接口读取graph_version，按 `(CID, edge ID)` 遍历接口返回的可达剧情，媒体按CID去重；设置500节点、5000选择、90秒总预算和单请求15秒/4MB限制。仅端点HTTP404/405/501允许从WBI player回退一次，不因403/429/5xx反复换接口。错误使用脱敏分类，不返回响应正文或Cookie。
- BFB仅对互动视频增加片段清单探测。清单完成标记、连续分P和CID顺序摘要必须一致；实际下载在选流前重新比较摘要。断点按CID复用成品，测试覆盖首轮2/3片段保留、第二轮仅补P3、第三轮不再下载，以及旧工具缺少完成标记时原清单不变、剧情清单变化时停止下载。
- 媒体探测输出预期剧情摘要，缺失任何片段时拒绝提供不完整的合计大小；普通视频不增加剧情接口请求，不修改上传、删除、播放或严格规格成功条件。摘要只针对CID列表，不是视频内容哈希；不重建互动玩法或隐藏条件状态。
- WSL BBDown单元测试61项通过，CLI构建通过（现有nullable/过时API警告保留）；BFB最终完整测试569项：567通过、0失败、2项既有环境跳过（aria2c缺失、Windows软链接/junction权限）。下载专项37项全通过。BFB应用构建、CLI Playwright三视口102通过/63条件跳过、文档安装与构建、两个仓库的 `git diff --check` 均通过。
- 根项目生产依赖审计12项（6 moderate、6 high），文档生产依赖0项。文档安装的全依赖审计另有12项开发工具链风险（9 moderate、3 high），未执行自动修复或升级依赖。
- 首轮实现保持BFB `2.5.4`、SQLite 11、状态13、迁移包3，当时固定BBDown为 `bfb-2.0.4`。随后已完成fork发布和本地dev接线，见上节；线上任务仍未修改，不能将本地验收宣称为线上恢复。

## 2026-09-06 v2.5.4 发布验收

- 应用和根锁文件版本统一为 `2.5.4`；本次为恢复、可用性探测账号边界、路径迁移交互和待处理展示修复，SQLite `user_version 11`、JSON 状态 schema 13、迁移包 schema 3 和 BBDown `bfb-2.0.4` 均不变。
- CHANGELOG、README及当前版本文档已同步更新；`v2.5.3`历史记录保留，`v2.5.3`到`v2.5.4`不执行数据库结构迁移。
- 完整 `npm test`：564项，562通过、2项按本机环境跳过（缺少 `aria2c`、Windows无法创建软链接/junction），0失败。
- CLI Playwright三视口：102通过、63项按视口条件跳过，0失败；应用构建、文档站构建和 `git diff --check`通过。
- 根项目生产依赖审计仍有12项既有上游风险（6 moderate、6 high，部分暂无修复），文档站生产依赖审计为0项；本轮未升级依赖。
- 当前发布提交先在 `dev` 验证，随后再由 `main`、`v2.5.4`标签和正式镜像使用同一发布提交；未改变已有数据库数据。

## 2026-09-05 可用性账号边界与前端复核修复

- 普通可用性探测仅使用活动收藏所属账号及UP主账号；充电意图保留其他账号回退。手动复核复用相同的账号边界，无相关可用账号时返回409。旧版测试中“普通探测回退到无关账号”的要求已随本轮策略更新。
- 收藏夹详情与归档库使用“已归档 · 收藏夹显示失效”；源站待确认文案不再承诺未安排的后台工作，已验证归档不增加自动探测。详情内部按钮的Enter和空格不再冒泡触发播放。
- 路径迁移使用取消信号、页面代次和串行轮询；关闭后迟到响应不能恢复轮询。冲突列表复用已有接口，每页20条并多取1条判断下一页，支持失败重试，不探测或伪造目标大小。列表仅展示已有相对路径及安全摘要，不自动覆盖冲突。
- 迁移弹窗增加独立内容滚动区，保留透明标题、按钮区和既有sticky约定，避免冲突文本与底部操作重叠。桌面和手机截图已人工检查。
- 完整单元与集成测试564项：562通过、2项既有环境跳过；完整CLI Playwright三视口102通过、63项条件跳过。最后的手动充电意图衔接补充后，可用性/充电专项33项通过；滚动区调整后，UI专项及视觉规范15通过、6项条件跳过。期间旧账号回退断言和一次sticky约束失败均已修正并复测。
- 应用构建、文档构建及git diff --check通过。未修改依赖，本轮未重跑依赖审计或文档安装。保持2.5.3及所有schema不变，仅本地dev修改，未提交、推送或访问服务器。

## 2026-09-05 停止尝试的恢复投影修复

- 修复线上历史组合 `abandoned + awaitingManualRecovery=true`：待处理、到期复核、看板和任务领取统一排除停止状态；停止操作清除调度时间，写回使用条件更新阻止旧回调重新开启人工处理。
- job store启动、rebind及周期会话对账每批最多修正1000条矛盾任务，清除旧租约并保留任务历史、文件证据和已有清理授权。已停止会话代次保留投影占位，不补建旧代上传；同BV新代不继承停止意图。历史标记不能证明用户亲自删除过视频。
- 完整 `npm test`：563项，561通过、2项既有环境跳过。最终补充启动/rebind和迟到租约回调测试后，SQLite任务专项35项全部通过。CLI Playwright三视口93通过、63项条件跳过；应用构建、文档安装与构建通过。
- 根项目生产依赖审计12项（6 moderate、6 high），文档生产依赖0项；本轮未修改依赖或锁文件。保持2.5.3及所有schema不变，本地dev修改，未提交、推送或操作服务器。
- 复核后补齐编码替换父任务的直接写库入口保护，并用停止父任务加运行中替换状态的迟到回调夹具验证 `finishEncodingRetry`、`updateEncodingRetry` 和重新开始均不会恢复人工处理或创建子任务。

## 2026-09-05 代码复核后补修

- 编码重试成功提交先验证父代次，只完成当前子任务；存在同代未完成子任务（包括人工等待）时保留父任务，不提前记录整体成功或触发整体清理。
- 清理循环退出后再次比较完整下载清单；发生变化时不写回旧快照。清单删除和最后一次身份判断不再跨异步间隔。
- 手动释放增加独立授权：从视频、任务和传输会话的本地目录解析清单文件，预览绑定文件身份和传输代次。未形成远端成功证明的成品也可由用户明确删除，自动清理仍只使用成功授权。活跃任务必须先停止，不接受前端任意路径，未知文件保留。
- 新增多子任务完成、清单更新竞态、无远端证明的明确删除回归。完整测试557项：555通过、2项既有环境跳过；补充运行任务拦截断言后清理专项16项通过。既有CLI Playwright三视口90通过、63项条件跳过，新增唯一副本危险确认三视口3项通过。应用构建、文档构建和`git diff --check`通过。本轮未变更依赖，未重新执行依赖审计；未提交、推送或访问线上。

## 2026-09-05 归档一致性与恢复可靠性收口

- 上传代次在本地路径、文件类型和大小验证后建立；`ensurePrepared` 将代次、分P记录与任务绑定放在一个事务内。缺失文件不再重开已完成会话，事务失败不会留下空代。
- 只读确认从持久化分P清单读取路径与预期大小，不要求本地成品仍存在；同大小仍需本代PUT证明，不允许由同BV的其他归档推定成功。
- 普通上传与会话确认增加事务化成功收尾和执行权检查；传输层不再递归删除本地目录。底层清理未传显式集合时不删除；文件变化或删除失败时保留记录。停止操作仅停止执行。
- 空代有完整旧代证明时执行只读远端核验，核验一致标为`superseded`，不搬用旧代分P。证据不足或未知时保留人工入口。停止意图不会被旧任务归一化清除。
- 历史证据限制：`BV1kg8Y6zEnZ`已确认本地视频缺失且无远端成功证明，但具体删除路径尚未证实；`abandoned`不能单独证明用户亲自删除过文件。本轮不连接线上或修改历史记录。
- 本阶段验证：完整`npm test`为537项，535通过、2项环境跳过；最后收紧空目录删除后，下载清单专项34项通过。CLI Playwright三个视口90项通过、63项条件跳过；应用构建、文档安装/构建和`git diff --check`通过。根项目生产审计12项风险（6 moderate、6 high），文档站生产审计0项；未升级依赖。测试通过不代表下列剩余功能已经实现。
- 后续收口：清理授权改存原有任务JSON，记录下载清单身份、传输代次、逐文件大小及文件系统身份。业务完成后保留不可领取的`completed`记录，释放原业务去重键；清理完成才删除记录。启动与rebind继续按分页恢复，不新增表或schema。
- 远端核验后逐文件重新检查共享任务、当前代次和文件身份；同大小替换、新任务引用、新代次和过期清单均保留文件。文件已删除但清单收尾前退出，可在恢复时幂等完成清单和授权收尾。没有清理授权时，即便同BV已有验证归档也不删除本地成品。
- 全部分P已验证但业务未收尾的孤立会话进入只读确认，不再误归为人工失败或重新建立上传代次。
- 本轮验证：完整单元与集成测试544项，542通过、2项环境跳过；之后补充代次变化和unlink后退出边界，清理与下载清单专项46项全部通过。CLI Playwright三视口90通过、63项条件跳过；应用构建、文档安装与构建通过。生产审计根项目12项（6 moderate、6 high）、文档站0项，没有自动更新依赖。所有测试均为本地隔离数据。
- **本轮收口完成**：画质重调的下载→上传→替换→清理阶段切换，以及编码重试的下载→上传→确认→父任务完成均收进SQLite事务边界；失败事务整体回滚。编码重试的活跃子任务枚举明确排除父任务和已完成的清理授权记录，多目标子任务ID不会被后续阶段覆盖。
- 本地归档完整性不再以“目录存在”推断：下载清单逐文件核对路径、普通文件类型和大小，能够区分完整、缺失、变化和未知，并在编码替换失败摘要中只陈述已经证明的本地状态与保留字节数。
- 归档详情新增“释放本地空间”危险操作。它只消费归档提交时已经持久化的清理授权，预览ID绑定BVID、下载清单、传输代次、授权ID和逐文件身份；真正执行前重新核验本地文件、远端大小、当前代次和活动任务。旧页面、目录复用、同大小替换、未授权文件或活跃任务均不会触发删除。
- B站来源探测补齐全部启用账号回退：按UP主账号、当前收藏账号、其他关联账号、其余启用账号排序并按实际UID去重；任一账号恢复后只通过既有`shouldEnqueueBackup`/幂等任务路径继续。持久复核计划在重复启动补偿时只生成一个`access_probe`。
- 最终可靠性专项回归257项：256通过、0失败、1项因当前Windows环境无法创建测试软链接/junction跳过。完整`npm test`共554项：552通过、0失败、2项按本机环境跳过；CLI Playwright共153项：90通过、0失败、63项按视口/项目条件跳过。`npm run build`、`npm --prefix docs run docs:build`和`git diff --check`全部通过。所有测试均使用本地隔离数据/Fake远端；未访问或修改真实B站、AList/OpenList、BBDown仓库或远端归档。SQLite schema 11、JSON state schema 13、迁移包schema 3和BBDown `bfb-2.0.4`均未改变。

## 2026-09-04 视频不可用软终态

- `view/detail`与`view`结果使用`available/unavailable/unknown`三态聚合：任一接口取得有效CID即为可用，两个接口都明确返回`-404/62002`才确认不可用；混合超时、5xx、未知业务码和空分P均保持未知。登录失效和风控继续走账号冷却，不消耗视频复核轮次。
- 收藏夹首次返回失效仅写入`pending_confirmation`并暂停新下载；明确不可用按1天、7天、30天复核，之后进入无定时任务的`dormant`。收藏夹重新返回正常条目会直接恢复；相关收藏账号或UP主账号重新登录时只做一次休眠唤醒，仍不可用则保留原轮次继续休眠。
- 下载任务的`BILI_VIDEO_UNAVAILABLE`只终止当前候选，不进入普通重试，不创建上传任务。严格画质/编码任务保留旧归档并结束本次候选；已有已验证分P继续留在本地清单，不会被当成完整上传结果。
- 旧版失败恢复只识别BFB历史固定文案和以`BBDown reported failure:`开头的明确视频不存在信息；WebDAV、权限、工具和本地文件错误不会被可用性恢复清除。`Arg_KeyNotFound`与“未找到此 EP/SS”按BBDown工具错误处理，不再作为下架证据。
- 新增`tests/video-availability.test.ts`，覆盖三态分类、固定退避、首次待确认、明确不可用、作者账号优先、多账号恢复、休眠、登录唤醒、意图合并、旧失败精确恢复、无账号手动复核和BBDown启动前拦截。所有测试使用隔离SQLite、Fake探测器和不可执行的BBDown命令，不访问真实B站或远端存储。
- 最终验收：`npm test`共526项，524项通过、2项按本机环境跳过（缺少`aria2c`、Windows无法创建软链接/junction）；`npm run test:ui`共153项，90项通过、63项按视口项目条件跳过；应用构建、文档站安装与构建、`git diff --check`均通过。根项目生产依赖审计报告12项既有上游风险，文档站生产依赖审计为0；本轮未升级依赖。

## 2026-09-03 重命名预览与在线状态可靠性

- 重命名接口使用内存短期预览会话，默认有效期5分钟、最多保留8个；公开候选只带稳定的 `candidateId`，OpenList 返回的访问写法和预览文件大小仅保存在服务端。重复预览复用未消费会话，配置身份、归档根、扫描上限或命名模板变化会拒绝旧会话。
- 远端深扫使用固定的条目与目录上限，并通过规范化访问路径集合跳过重复目录。完整结果替换本地证明候选；扫描失败或未完整覆盖时保留本地已验证候选，执行前仍逐项重新读取远端。
- 前端选择状态按 `candidateId` 保存，轮询无 revision 变化时只更新扫描状态，不重绘候选列表；关闭预览、开始重命名或切换请求时会取消旧轮询和请求。
- 在线内容页面缓存不再保存 `archiveState`，每次返回都用一次 SQLite 批量投影刷新 `archived/processing/unarchived/unavailable`，因此缓存命中不会掩盖刚完成的手动归档或队列状态。
- 新增隔离测试覆盖预览会话复用、过期/消费幂等、完整扫描权威替换、扫描边界与重复目录、OpenList 访问别名、在线缓存状态刷新，以及应用接口拒绝旧式原始路径请求。测试只使用内存/隔离目录和 Fake 远端，不连接真实 B 站、AList、OpenList 或删除服务。
- 最终验收：`npm test` 共 510 项，508 项通过、2 项按本机环境跳过（缺少 `aria2c`、Windows 无法创建软链接/junction）；`npm run test:ui` 共 147 项，85 项通过、62 项按视口项目条件跳过；应用构建、文档站安装与构建、`git diff --check`均通过。根项目生产依赖审计当前报告 12 项既有上游风险，文档站生产依赖审计为 0；本轮未升级依赖。

## 2026-08-28 v2.5.3 版本更新

- 应用和根锁文件版本统一为 `2.5.3`；本次只发布预览性能与查询收口，SQLite `user_version 11`、JSON 状态 schema 13、迁移包 schema 3 和 BBDown `bfb-2.0.4` 均不变。
- CHANGELOG、README 及当前版本文档已同步更新；`v2.5.2` 历史记录保留，`v2.5.2` 到 `v2.5.3` 不执行数据库迁移。
- 当前仍在 `dev` 分支，尚未创建 `v2.5.3` 标签、合并 `main` 或推送本次版本提交。
- 本轮验收：`npm test` 共503项，501项通过、2项按环境跳过（缺少`aria2c`、Windows无法创建软链接/junction）；`npm run test:ui` 共147项，85项通过、62项按视口条件跳过；应用构建、文档站构建和`git diff --check`均通过。
- 两处生产依赖审计结果保持不变：根项目11项既有上游风险，文档站0项；本轮没有执行依赖升级。

## 2026-08-28 预览速度优化

- “检查旧命名文件”现在先从SQLite的已验证远端证明构造本地索引结果，首个响应不再等待整棵远端目录树；后台远端扫描使用最多2个并发目录读取，结果在进程内短时复用并通过`/api/rename/preview/status`补充。扫描失败或中断时保留本地结果，真正执行前仍使用既有远端逐项复核。
- 重命名和画质重调预览的跳过项都保留总数与原因统计，页面最多展开50个样例；画质任务目标一次查询，避免逐关系访问持久化任务表。
- 页面只每秒轮询一次后台扫描，并在扫描达到深度/数量上限时明确标记为未完整覆盖；本地与远端扫描合并时优先使用完整远端摘要，避免同一跳过项重复计数。
- 已新增隔离单测覆盖预览缓存去重/过期/失败收敛、扫描并发上限、跳过汇总、已验证证明去重和未验证证明排除；最终命令结果记录在本节末尾。
- 本轮最终验收：`npm test`共503项，501项通过，2项按本机环境跳过（缺少`aria2c`、Windows无法创建软链接/junction），0项失败或挂起；`npm run test:ui`共147项，85项通过、62项按视口项目条件跳过，CLI Playwright未连接真实服务；应用构建和文档站构建通过，`git diff --check`通过。
- 根项目生产依赖审计仍有11项既有上游风险（2低、3中、6高，部分无可用修复），文档站生产审计为0项；本轮未执行依赖自动升级。

## 2026-08-26 弹窗与在线内容表面收口

- 普通弹窗的标题区和底部操作区使用透明表面并关闭子层模糊，直接显示弹窗主体的玻璃背景；粘性定位、边框、按钮和移动端底部面板行为保持不变。
- 在线内容桌面端的刷新按钮不再被`space-between`推到标题与关闭按钮之间的中点，而是与关闭按钮组成右侧操作区；侧栏只保留“在线内容”标题，移除重复的读取说明。
- CLI Playwright隔离浏览器检查了`1280x792`桌面渲染：刷新按钮位于关闭按钮左侧且间距小于32px，侧栏标题文本仅为“在线内容”；未连接真实B站、AList/OpenList或删除服务。
- 构建、在线内容专项和视觉系统专项测试通过；最终完整测试结果以本次提交前的命令输出为准。

## 2026-08-26 收藏夹选择器封面缓存

- 收藏夹列表接口通常只返回标题和数量，选择器现在通过受保护的单收藏夹详情接口按可见项读取封面；图片统一进入`data/online-covers`，沿用256MB上限和LRU清理，不进入永久归档封面目录。
- 同一UID与收藏夹的图片缓存命中时不再访问B站；收藏夹列表结果在进程内短暂复用，列表响应已有封面会跳过详情元数据请求，详情请求按账号最多并发两个，失败只保留10秒短暂负缓存。没有封面时前端保留固定占位，不影响勾选和保存。
- 在线内容图片响应缓存为24小时，服务端懒加载引用有效期略长于该周期，避免卡片长时间未滚动到视口后因引用过期出现误报占位。归档封面命中还会检查普通文件、非空和可读权限，但不在每次读取时执行ffprobe。
- 隔离测试覆盖缓存命中不重复查询、同一收藏夹并发请求合并，以及选择器使用同源图片接口；未连接真实B站、AList/OpenList或删除服务。

## 2026-08-26 v2.5.2 发布验收

- 根项目版本与锁文件已统一为 `2.5.2`；本次不改变 SQLite schema 11、JSON 状态 schema 13、迁移包 schema 3，也不更新 BBDown `bfb-2.0.4`。
- 在排除既有预览服务、`node_modules`和临时产物的全新隔离副本中完成 `npm ci`；`npm test` 共 487 项，485 项通过，2 项按本机环境跳过（缺少 `aria2c`、Windows 无法创建软链接/junction），0 项失败或挂起。
- CLI Playwright 共 147 项，85 项通过、62 项按视口项目条件跳过；桌面、手机和横屏项目均完成，测试只使用隔离假API，不连接真实B站、AList/OpenList或删除服务。
- `npm run build`、`npm --prefix docs ci`、`npm --prefix docs run docs:build`和`git diff --check`通过。根项目生产审计仍有11项既有上游风险（2低、3中、6高，无可用修复的项目），文档站生产审计为0项；未执行强制依赖升级。
- 原工作区的首次 `npm ci` 曾因既有本地预览服务占用 `esbuild.exe` 返回 Windows `EPERM`，未关闭其他进程；隔离副本的干净安装和全部验收已成功完成。

## 当前基线

- BFB `v2.5.3` 已在 `dev` 完成版本更新；`v2.5.2` 于 2026-08-26 完成上一版发布验收，`v2.5.3` 仍需在推送和发布前完成本轮验收。
- BBDown fork 固定为 `bfb-2.0.4`，源码提交 `0ea9463202e8a57e0d673f29166e54f4ed770255`，Linux x64 ZIP SHA256 为 `74a944d25f5e29528b93e0bfebadbae7d0cc7cc1567cda531a26ed4d4e8dfa9a`。
- SQLite 为 `user_version 11`，JSON 状态 schema 13，迁移包 schema 3。
- 当前发布验收完整 `npm test` 共 487 项，485 项通过，2 项按本机环境跳过；CLI Playwright 共 147 项，85 项通过、62 项按视口条件跳过。应用构建、文档构建和差异检查均通过。
- GitHub 首轮干净环境暴露的媒体探测工作目录和跨平台 RSS 门槛已在 `fb0719a` 修复；修复后的 `dev`、`main` 和标签 Docker 工作流全部通过。
- 根项目生产依赖审计保留 11 项上游风险，文档站生产依赖为 0 项；未执行破坏性自动升级。
- 本次发布未部署 Aliyun，也未访问真实 B站、AList/OpenList 文件。

## 2026-08-25 发布后dev验证

- 待处理媒体选择器已扩展为画质×编码矩阵。真实账号的隔离探测验证了两分P视频可按共同组合分页聚合，并在严格选择`1080P / AV1`时返回`1920x1080`、`24fps`、预计成品和本地峰值；CDN没有提供可靠总长度时明确保留`bitrate_estimate`来源，不冒充HEAD/Range精确值。
- B站对`BV1kg8Y6zEnZ`返回稿件不可见时，界面不会虚构可用AV1或分辨率，不预选回退项，提交按钮保持禁用；用户仍可进入手动严格尝试，但必须明确选择至少一个规格。
- `npm test`共471项，469项通过，2项仅因本机缺少aria2和Windows软链接权限跳过。完整CLI Playwright共96项，61项通过、35项按视口条件跳过；画质×编码、大小精化、严格组合不回退和看板三视口均已覆盖，且未连接真实WebDAV或触发下载上传。
- Aliyun验证使用只读生产封面和复制的SQLite/账号配置，存储地址指向不可达本地端口；`NODE_ENV=test`阻止调度器领取任务。仅媒体探测访问B站，没有调用真实OpenList、创建下载任务或写入生产数据。

## 候选恢复与405诊断专项

- 恢复投影使用现有`jobs`和`transfer_sessions`，按`sessionId:generation`幂等补建任务；隔离测试覆盖失败会话无任务、重复打开待处理、数据库重载和终态任务在会话仍未完成时重新投影。
- 隔离Fake WebDAV覆盖异大小冲突、多分P部分存在、三次/30分钟远端不可见门槛和候选失败后的停止自动套娃；正式路径不被覆盖，已验证旧档仍可用。
- 405诊断只使用通用WebDAV响应，不读取OpenList日志。测试覆盖`X-OpenList-Error-Code`、`Allow`、`Retry-After`和敏感认证头过滤；明确`SingleFileSizeOverLimit`才分类为单文件限制，普通405不自动降画质或切私有API。

## 候选级恢复补充验证

- 传输会话恢复投影现在按分页读取，隔离夹具使用超过1000条活动会话验证了后续孤儿会话仍会生成唯一的`upload-session:<sessionId>:g<generation>`入口；重复启动、重载数据库和打开待处理中心不会重复创建任务。
- 多分P夹具验证了`partial_upload`会保留已确认分P、显示`verifiedPages/totalPages`，重试只处理缺失或未知分P；投影阶段把本地状态保留为`unknown`，权威文件检查仍在实际恢复动作中执行，避免请求页面时同步阻塞`stat`。
- “放弃本次候选”已覆盖会话代次收尾、候选审计留痕和重复请求幂等；它不会删除本地或远端文件，也不会改变旧归档证明，后续同步可以创建新的候选代次。
- 旧版永久下载失败使用独立隔离记录验证为统一待处理项；用户明确放弃后不再被每轮同步重新投影，未放弃记录仍可按原账号、备用账号或暂缓操作恢复。
- 本轮新增看板UI夹具验证部分分P文案和确认入口；CLI Playwright仍只使用Fake API和隔离数据，不连接真实B站、WebDAV或删除服务。

## 2026-08-26 前端请求状态专项

- 收藏夹账号切换、在线搜索、归档筛选、登录轮询和初始加载均使用隔离Fake API验证迟到响应与失败恢复；账号启停和立即同步的快速双击只产生一个请求，启停请求携带明确的`enabled`目标值。
- 弹窗关闭后保持`hidden + inert + aria-hidden`，隐藏控件不进入Tab顺序；归档库替换请求期间旧结果不可操作，失败后恢复上一次目录、查询、汇总和分页游标。
- `npm test`共479项，477项通过，2项仅因本机缺少aria2和Windows软链接权限跳过。CLI Playwright共129项，76项通过、53项按视口条件跳过，覆盖`1280x720`、`390x844`和`844x390`，没有新增控制台错误或404；测试未连接真实B站、WebDAV或删除服务。

## 2026-08-26 前端视觉系统专项

- 现有静态HTML/CSS/JS继续保留，不引入前端框架或动效依赖。主界面采用连续的操作工作区，普通弹窗收敛为`sm/md/lg/xl`四档；归档库、在线内容和待处理中心共用浅青绿色玻璃工作区外壳，播放器保留独立深色主题。
- 统一设计令牌覆盖控件、卡片和弹窗的`6/8/10px`圆角、半透明面板、背景光晕、柔和阴影、边框、焦点、主次及危险按钮。普通弹窗标题和操作区使用粘性定位，手机端使用视口内底部面板；现有弹窗栈、请求取消、焦点恢复、接口和业务状态机未修改。
- 主页外层卡片使用`10px`圆角，主页操作按钮和页头按钮使用`8px`圆角，帮助按钮同步处理；弹窗、播放器和归档库不跟随主页圆角变化。设置操作区后面的`configStatus`、`renameStatus`和`qualityUpgradeStatus`为空时使用`display:none`，只有实际提示出现时才重新占据布局空间，因此不会再在按钮下方留下大块空白。
- 新增CLI Playwright视觉契约，检查全部普通弹窗的尺寸类和共同外壳、危险按钮顺序、三视口边界和横向溢出，以及归档/待处理工作区与播放器主题隔离。手工截图使用隔离Fake API，不连接真实B站、WebDAV或删除服务。
- 本次收口后重新验证：`npm run build`通过；视觉契约测试`6`项通过、`6`项按项目条件跳过；`git diff --check`通过。完整功能测试和三视口UI回归基线仍为`479`项中`477`项通过、`2`项按本机环境跳过，以及`141`项中`82`项通过、`59`项按视口条件跳过。
- 完整`npm test`共479项，477项通过、2项仅因本机缺少aria2及Windows软链接权限跳过；CLI Playwright共141项，82项通过、59项按视口条件跳过。应用构建、文档构建和`git diff --check`通过；根项目生产依赖仍为既有11项上游风险，文档站为0项。

## 2026-08-26 空状态与在线内容展示专项

- 待处理中心没有人工事项时隐藏“自动复核中”摘要徽标，并把空状态内容固定在同一布局行，避免隐藏状态节点触发空白行和不同背景色；桌面弹窗使用视口安全边距，移动端继续使用全屏面板。
- 在线内容关闭控件按桌面/手机工作区职责区分：桌面只保留内容区主关闭按钮，手机目录页显示侧栏关闭按钮，内容页使用主区关闭按钮。两级视图切换继续同步`hidden`、`inert`和`aria-hidden`。
- 在线收藏夹导航复用 B 站页面返回的`media_count`，并使用页面总数补充当前分类摘要；无可靠总数时才显示“打开时加载”。数值均通过DOM文本节点写入，不拼接标题HTML。
- 追加隔离回归：在线导航数量、桌面/手机关闭按钮、待处理空状态布局和隐藏状态节点均覆盖；`npm test`共480项，478项通过、2项因本机缺少aria2及Windows软链接权限跳过；CLI Playwright共144项，84项通过、60项按视口条件跳过；应用与文档构建、`git diff --check`通过。

## 2026-08-27 画质预览与OpenList旧命名专项

- 画质升级预览改为先筛选`verified/partial_verified`关系，再解析远端文件证明；无文件、非法/越界路径、多目录文件或目录证明不一致均按单项跳过，不影响其他候选。纯函数夹具覆盖空证明、缺失目录推导、多分P同目录、跨目录、越界和反斜杠旧路径。
- 远端扫描条目明确区分`accessPath`与严格路径。OpenList的`\'`、`\%XX`访问写法不进入API或SQLite；特殊文件只有在同目录、规范名称和大小与本地证明唯一一致时才映射，NFC/NFD多匹配、转义/未转义双条目和未知文件均安全拒绝。
- 批量重命名在所有远端写操作前重新读取父目录；高风险名称强制走唯一目录匹配，即使直接`stat`成功也会检测别名冲突。首个MOVE/COPY使用服务端观察到的访问路径，临时路径、目标路径和SQLite写回继续使用严格逻辑路径；MOVE不支持时的COPY+验证+DELETE及批次回滚语义保持不变。
- 专项测试共71项通过；最终完整`npm test`共498项，496项通过，2项按本机环境跳过（缺少`aria2c`、Windows无法创建软链接/junction），0项失败或挂起。CLI Playwright共147项，85项通过、62项按视口项目条件跳过；应用构建、文档安装与构建、`git diff --check`均通过。根项目生产审计保留11项既有上游风险（2低、3中、6高且多数无可用修复），文档站生产审计为0项；未执行破坏性依赖升级。

## 后续规则

- 新改动追加到 CHANGELOG 的“未发布”章节，并在本文件记录专项测试和已知边界。
- `main` 只接收已经在 `dev` 和隔离环境验证过的线性提交，不强推、不改写发布历史。
- `output/` 和 `.review-*` 是本地未跟踪审查产物，不属于提交或发布内容。
