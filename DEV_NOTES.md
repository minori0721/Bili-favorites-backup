# Dev 测试说明

## 当前基线

- 基准版本：`v2.4.6`，发布提交：`2da980b`。
- 当前dev在发布基线上增加归档库分页、投影、恢复、前端竞态收口和直接上传恢复。
- SQLite：`user_version 10`；JSON兼容状态：schema 13；迁移包：schema 3。
- BBDown固定为fork Release `bfb-2.0.1`，源码提交`fd926373dfe03d68bf84a1ad8a4ffbf402b00988`。

## 当前dev改动

- 远端兼容性收口：上传 PUT 收到405时只在精确路径已存在、类型为普通文件且大小一致时标记成功；缺失或大小不一致仍进入原有失败/补传流程。新增测试覆盖实际落盘405、未落盘405和大小冲突。
- 直接上传会话：普通归档只向正式路径条件PUT，不创建`.bfb-staging`，不依赖MOVE；SQLite保存会话和逐文件状态，重启后优先确认同一正式路径。已完成会话在远端文件被移除后会原地重开并递增上传代际，旧逐文件明细保留，新任务只读写当前代际。PUT已接受但暂不可见时不自动重复正文传输；多分P分别从各自PUT接受时间计算10分钟确认窗口，等待复核只`defer`任务而不消耗重试次数。超过窗口后队列显示“重新确认 / 继续上传”；重新确认只读检查，继续上传的授权按文件持久化，并在对应文件即将PUT前原子消费。带传输会话的确认任务按会话和代际只创建一个，并且不会继承人工重传权限；未知同大小和异大小目标都不会冒认为本次上传成功。
- 待处理与自动自愈：`/api/queue/state`返回结构化`issues/issueSummary`，前端提供独立问题中心。后台每5分钟由SQLite直接选取最多25个已到期人工等待任务；远端同大小自动确认，本地失效且远端全缺失时只自动重下1次，并把恢复次数沿下载、上传和确认任务持久传递。手动“立即重新检查”严格只读；与后台复核并发时共享同一次远端检查，避免重复请求或依据旧结果重下。异大小、混合分P、认证错误和无法安全重建只生成问题项，不自动写远端。命令行Playwright覆盖三个视口、焦点限制、手机返回和操作防重。
- 确认冲突收口：上传确认阶段的确定性409会结束当前`verify_upload`，按远端冲突路径定位分P并创建带`sessionGeneration`的人工恢复项；旧确认任务、旧上传任务或旧代际回调不能更新新代际。专项覆盖确认409、旧代际拒绝、重开保留历史明细以及schema 9到10迁移。
- 兼容文件名仍然有效：持久会话遇到驱动拒绝四字节文件名时，使用不冲突的兼容文件名重新PUT，并把最终路径写回会话；整个过程不调用MOVE。
- 路径迁移开始前使用当前目标根下的随机临时目录分别探测 COPY 与 MOVE，探测只使用隔离文件并在结束后清理；COPY不可用时返回409且不创建真实迁移复制项。OpenList 和 AList 均通过同一 WebDAV 客户端接入，配置字段名保持兼容。
- 隔离远端探测结果：AList v3.63.0 天翼驱动 PUT 返回405但文件实际写入，COPY返回500；OpenList beta普通 PUT、MOVE可用，但同一驱动COPY返回500。测试文件使用随机前缀，已清理；没有访问业务归档、没有执行真实路径迁移、没有重启容器。
- 远端替换兼容：新增独立的 COPY/MOVE 探测和可恢复替换执行器。画质升级、批量重命名优先 MOVE；仅在 MOVE 明确返回不支持且 COPY 目标大小校验通过时使用 COPY+DELETE。普通上传继续直接 PUT，不创建暂存目录，也不依赖 MOVE。
- 旧档优先与冲突候选：普通同步捕获当前收藏来源的旧归档证明并整组预检。完整旧证明重新核验一致时保留旧档、废弃本地新版上传尝试；证明不完整、远端变化、未知同大小或异大小时，不移动正式路径，而是把完整本地分P组PUT到稳定的`_conflicts/upload-<任务ID>`目录。候选重试复用同一任务路径，问题中心串行执行“保留现有 / 采用候选”；采用候选后旧文件不删，旧证明和媒体元数据保存在候选审计记录中。
- 替换恢复：MOVE响应丢失时通过源消失、目标存在且大小一致认定成功；COPY+DELETE会在目标大小校验后先持久化本次操作证明，再删除源文件。画质升级的备份/新版路径会即时写回任务载荷；重启后只有该证明存在时才复用同大小目标并补删源文件，无证明的既有目标一律按冲突保留。路径迁移的旧目录级`DELETE`逻辑本轮未修改。
- 维护锁与导入：持久传输任务的`pending/retry_wait/leased/running/manual_wait`均视为未完成；只要仍有这类任务，迁移导入会在接收和替换SQLite前返回409，避免轻量导入清空人工恢复项、传输会话或本地缓存关联。
- schema 8新增全局和账号范围的`archive_library_projection`持久投影。schema 7升级前生成一致性备份与SHA256，在同一事务中完成建表、索引和全量投影；写入后按脏BVID增量刷新。收藏夹范围继续实时读取关系顺序。
- 历史收藏夹游标显式保存顺序是否已知，不再把缺失顺序映射为超大JavaScript整数；不安全旧游标返回`ARCHIVE_CURSOR_STALE`并由前端自动重置一次。
- 归档列表当前页的来源、删除状态和远端统计改为批量CTE水合；播放器焦点定位移除全量窗口排名，相邻页使用双向键集游标，旧数字页保持兼容。
- 当前登录的同UID账号会在启动和数据库重载后恢复遗留归档快照、脱离关系及暂停任务，但未完成或失败待处理的账号删除会继续阻止恢复。
- 归档目录、列表、账号删除进度和来源删除启动响应按会话、账号、操作及详情代次隔离，迟到响应不能覆盖新打开的弹窗或详情。
- schema升级日志仅在真实升级时记录版本迁移、成功或失败及耗时；日志不包含数据库路径、备份文件名、SQL错误正文或业务数据。
- OpenList文档补充了外接部署、`/dav`基础地址、权限动作、405复核、COPY迁移边界和临时测试清单；内置Compose继续保持AList，兼容字段名不变。
- 1万个唯一BV、5万条关系的压力夹具完成全量分页且无重复遗漏；schema 8一次性投影构建本机约21秒，归档库综合压力测试约25秒。新增上传会话均使用隔离目录和Fake WebDAV，测试未连接真实B站、AList或服务器。

## 发布验证基线

```bash
npm ci
npm test
npm run test:ui
npm run build
npm --prefix docs ci
npm --prefix docs run docs:build
git diff --check
npm audit --omit=dev
npm --prefix docs audit --omit=dev
```

- 当前dev本地逻辑测试共332项：331项通过，1项因本机没有`aria2c`跳过；本轮新增直接PUT、无MOVE、条件竞态保护、逐分P确认窗口、会话级确认、逐文件人工重传授权、兼容文件名、持久目标证明和问题中心自愈专项测试，并覆盖只读人工复核、并发复核合并、1000条未到期事项后的到期任务选择、旧会话阶段废弃、授权重传失败后的人工恢复停车、持久任务导入保护，以及画质升级在COPY成功但DELETE失败后跨任务恢复且不重复COPY。命令行Playwright共36个项目用例：18项按适用视口执行通过，18项按测试条件跳过，无失败。
- 应用与文档构建通过。根生产依赖审计为11项（2项低危、3项中危、6项高危、0项严重），主要来自现有上游传递依赖；文档站为0项。
- schema 10回滚必须恢复`data/backups`中的schema 7/8/9升级前对应备份；schema 9及更早镜像不能直接打开schema 10数据库。普通上传不需要MOVE；画质升级、批量重命名和路径迁移会在实际操作前按各自规则检查服务器端能力，路径迁移的目录级清理行为保持原样。

## 后续合并规则

- 新改动追加到CHANGELOG的“未发布”章节，并在本文件记录专项测试和已知边界。
- 发布时main仅使用`--ff-only`接收dev，整理正式版本和文档后删除本文件。
- 发布完成后dev快进main，再重建精简基线说明；不挑选、压缩或改写已经验证的功能提交。
