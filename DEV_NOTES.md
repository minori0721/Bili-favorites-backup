# Dev 测试说明

## 当前基线

- 基准版本：`v2.4.5`。
- main发布提交：`ab9f59fbae30e4ac20fc5bb8aa03e66bb7affc99`。
- SQLite：`user_version 7`；JSON兼容状态：schema 13；迁移包：schema 3。
- 当前工作区新增账号移除与归档库来源安全删除：默认只移除登录，危险模式使用持久`archive_delete`任务；物理路径按证明去重、全量HEAD预检、逐文件DELETE、共享保留、重启恢复和最小审计均已实现。BFB不再删除任何远端目录，允许留下空目录以彻底避开未知文件并发写入竞态。
- 账号远端清理先原子进入`preparing`并持有维护锁，再等待在途任务退出、裁剪共享任务目标和移除登录；配置写入、任务裁剪、关系脱离和删除任务启动共享SQLite事务边界，失败会精确回滚。重复请求复用同一操作；启动或数据库rebind会根据账号是否仍存在，把中断准备恢复为重新确认或继续`pending`清理。
- 启动会为旧`account_detached_at`关系补建无凭据账号快照；正常全量扫描在没有删除留痕时不再进入SQLite写事务，1万次空恢复专项断言事务数为0。
- 归档库增加“已移除账号”和“已删除”范围。删除失败可直接重试；AList身份、归档根或本地证明变化后可重新预览并再次输入危险确认。
- 删除预览会复用相同目标仍有效且证明一致的快照，不滚动30分钟期限；到期或漂移预览立即清除文件与来源明细，仅保留24小时统计摘要。清理在启动、数据库rebind、创建预览前和30分钟定时器执行，`stop()`会释放定时器，运行中、失败、重试和完成任务均不受影响。
- 归档删除与归档库已完成首轮性能收口：执行中状态读取不再扫描任务表，物理路径共享判断使用`idx_remote_files_path`，逐文件进度改为原子递增，来源与视频聚合批量收尾；导航合并为一次分层汇总，收藏夹候选不再重复按BVID聚合，首屏页面与汇总复用同一候选集。前端账号删除进度正常只轮询轻量操作接口，终态或状态接口异常时才刷新完整导航。
- dev当前有未提交的APP下载配套改动：为每个账号持久化独立`appBuvid`，并通过BBDown私有临时配置传递，不进入WEB Cookie。
- 配套的本地`BBDown-src`工作区已将普通UGC迁移到PlayerUnite，合并AVC、HEVC和AV1结果；APP结果仅480P或低于明确请求档位时只择优合并更高的WEB视频，保留APP音频。PGC接口不变。
- BBDown fork已发布`bfb-2.0.1`，Release和标签指向`fd926373dfe03d68bf84a1ad8a4ffbf402b00988`；Docker已固定该Release、源码提交及ZIP SHA256 `341eef88483d0cd4461031beee5c8de54192f1427e698970e33b81c3ed933074`。
- BBDown提交升级时只复用配置未变化的WEB未完成轨道；APP未完成轨道会隔离后重新下载，避免旧480P半成品绕过PlayerUnite修复。已验证输出继续保留。
- 管理界面现使用统一`modalStack`维护弹窗层级、`inert`/`aria-hidden`、顶层Tab循环及逐层焦点恢复；归档详情额外隔离同级目录和结果区，关闭时优先返回原卡片，卡片消失则返回结果区。播放器与归档库不再各自修改父弹窗可访问状态。
- 归档来源删除在预览阶段即锁定按钮，通用危险确认使用固定处理器的单实例状态机；归档搜索以`draftQuery`和已生效查询分离，防抖期间的卡片操作继续绑定旧上下文，应用新查询或切换上下文时会关闭旧详情并取消迟到请求。
- 新增`@playwright/test`隔离假服务和`npm run test:ui`，不连接B站、AList或真实删除服务；浏览器回归保留为本地发布前检查，不加入Docker Publish工作流。
- 浏览器插件使用同一隔离假服务复核`1280×720`、`390×844`和`844×390`：确认框层级、焦点循环、逐层Escape和焦点返回正常；额外发现并修复手机弹窗内边距覆盖、目录容器横向偏移及封面零尺寸重叠，最终三个视口均无横向溢出、控制台错误或404。
- Windows完整测试不再全局切换进程工作目录，改为仅在`NODE_ENV=test`下通过内部`BFB_TEST_APP_ROOT`绑定隔离根目录，避免测试加载器或短命子进程继承待删除目录并造成`EBUSY`；生产根目录和关闭逻辑未改变。

## 发布验证

```bash
npm ci
npm test
npm run test:ui
npm run build
npm --prefix docs ci
npm --prefix docs run docs:build
git diff --check
npm audit --omit=dev
npm --prefix docs audit --omit=dev
```

- BFB完整逻辑套件共277项：276项通过，1项因本机未安装aria2跳过，0项失败。删除引擎、账号编排、归档库、播放、schema和静态前端专项测试均通过；关闭期限测试确认WebDAV挂起时不会提前关闭SQLite。
- Playwright交互套件在三个项目中执行21个项目组合，按视口选择9项有效场景并跳过12项不适用组合；9项全部通过，覆盖确认框层级和可点击性、Tab循环、Escape顺序、双击删除仅一次预览/启动、删除后焦点回退、失败账号清理重新确认、搜索防抖/取消及手机横竖屏返回。测试同时断言无控制台错误、资源404和稳定态横向溢出。
- 删除预览专项使用线上同构夹具验证9065条正常远端证明折叠为4533个物理文件；连续和并发预览只保留同一份4533行临时明细，证明表保持9065行。另覆盖配置、归档根、路径、大小与来源漂移、24小时摘要、启动/rebind/定时清理及非预览任务保护。
- 删除性能专项断言两个实际删除文件只执行预检同步和最终收尾共两次完整进度计数，`EXPLAIN QUERY PLAN`确认共享路径查询通过`idx_remote_files_path`执行索引查找；删除进入`pending`后读取状态不再查询普通任务表。
- 1万个唯一视频、5万条收藏关系的同构内存夹具复测：归档导航约184.9ms（优化前233.5ms），首个全局问题页约107.4ms（优化前227ms），后续9页约1.02s（优化前约0.93s，同一量级且本轮未改后续页路径），收藏夹首屏约35.4ms；汇总均保持1万个唯一BV且分页无重复遗漏。
- 使用独立命令行Playwright与隔离数据完成`1280×720`、`390×844`和`844×390`检查：历史来源删除入口、账号删除安全默认项、危险确认禁用/启用状态均正确，无稳定态横向溢出、资源404或控制台错误；未调用Codex浏览器插件，也未提交任何实际删除操作。
- BBDown .NET 9测试共14项，覆盖PlayerUnite请求、三编码合并、局部失败、空流信号、真实媒体字段、稳定buvid、请求日志脱敏和WEB视频择优，全部通过；NuGet直接及传递依赖漏洞检查为0项。
- BBDown GitHub Actions已完成Linux x64 NativeAOT构建、版本与无DLL校验并上传Release；Release工作流和14项.NET测试均通过。本机没有Docker，因此BFB镜像构建仍需由后续GitHub Actions验证。
- 根生产依赖审计为10项（3项低危、3项中危、4项高危、0项严重），文档站生产依赖审计为0项。
- main的`latest`镜像、GitHub Pages和`v2.4.5`版本镜像工作流均已成功。

## 后续合并规则

- 新开发内容先进入dev，并记录在CHANGELOG的“未发布”章节和本文件中。
- 发布时main只使用`git merge --ff-only`同步dev，不挑选、压缩或改写提交。
- main发布提交删除本文件；版本标签和main工作流成功后，dev再快进main并重建精简说明。
